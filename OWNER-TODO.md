# OWNER-TODO — единственный канал к владельцу

Активных пунктов: 5, суммарно около 17 минут. Всё остальное исполняется
агентом. Каждый пункт: что сделать, где кликнуть, ожидаемый результат.

1. Выбрать вариант позиционирования А или Б в docs/POSITIONING-PROPOSAL.md
   (5 мин) и обновить GitHub About готовым текстом из выбранного варианта
   (Settings → General → Description). Рекомендация исполнителя — А (не
   превосходит данные при adoption-ноль). Ожидание: About совпадает с
   выбранным вариантом; агент одним коммитом разнесёт формулу по семи
   поверхностям и прогонит гейт дословности.
2. Запушить подписанный тег v3.31.1 (3 мин): ветка release-3.31.1 влита в
   main, версия синхронна во всех носителях, CHANGELOG-секция на месте —
   `git tag -s -a v3.31.1 -m "3.31.1 — гигиена витрины, машинный слой
   скилла, безопасность MCP" && git push origin v3.31.1` (подпись тега GPG
   по GOVERNANCE.md). PyPI опубликуется автоматически (OIDC), Release и
   детерминированный архив соберёт агент сразу после тега. Ожидание:
   workflows «Проверка релиза» и «Публикация на PyPI» зелёные.
3. Личная GPG-подпись финального акта (2 мин, вместе с тегом):
   `gpg --clearsign drafts/FRONTIER-ACT.md` в ран-каталоге исполнителя —
   поверх исполнительской подписи (пункт П14). Ожидание:
   FRONTIER-ACT.md.asc с вашей подписью.
4. Glama: вход и submission MCP-сервера по черновику (5 мин) — вход через
   ваш GitHub-аккаунт, агент создать аккаунт не может. Ожидание: листинг
   сервера; после листинга агент добавит бейдж в README.
5. Smithery: `npm install -g smithery@latest && smithery auth login`
   (OAuth вашим аккаунтом), затем публикация по docs/build/publish
   (2 мин; совместимость проверена — stdio-сервер публикуется MCPB-бандлом
   или через репозиторий). Закрепить репозиторий в профиле GitHub
   (Pin repositories, 1 мин) тем же заходом. Ожидание: карточка сервера в
   реестре Smithery, репозиторий в закреплённых.

Закрытые пункты (история): About/Website формулой и Discussions включены
агентом через API 2026-09-04; лицензии MultiSocial/CoAT проверены живым
Zenodo API 2026-09-05 (фрагменты публиковать нельзя даже при доступе —
research/LICENSES-EXTERNAL.md); ротация токенов — долгоживущих токенов нет
(аудит), OIDC-доверие работоспособно (публикация 3.31.0/3.31.1); ответ
первому внешнему пользователю — по появлению (тред Discussions #95 открыт,
агент мониторит в режиме поддержки).
