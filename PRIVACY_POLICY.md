# Privacy Policy / Политика конфиденциальности

**humanizer-ru** — текстовый скилл для ИИ-агентов: набор файлов разметки (`SKILL.md`) и локальных скриптов для CI. У проекта нет серверной части.

## Что проект собирает

Ничего.

- **Скилл текстовый.** Он работает внутри среды агента: только читает и правит текст, не выполняет код, не обращается к сети и не передаёт данные третьим сторонам.
- **Нет телеметрии.** Никакой аналитики, сбора метаданных или отсылки обрабатываемого текста наружу.
- **CLI, MCP и Action обрабатывают данные локально.** Команды пакета
  (`humanizer-markers`, `humanizer-scan`, `humanizer-polish`,
  `humanizer-detect`, `humanizer-facts`, `humanizer-report`) читают только
  stdin и указанные вами файлы, работают офлайн и ничего не отправляют.
  MCP-сервер (`humanizer-mcp`, stdio) отвечает на JSON-RPC-запросы клиента
  без сетевых вызовов. GitHub Action обрабатывает diff в раннере вашего CI.
- **Валидаторы репозитория — не продукт.** Проверочные скрипты `scripts/`
  (гейты, экспорт, CI-инструменты) запускаются в CI репозитория или
  разработчиком; текстовому агенту они не нужны. Продуктовые команды
  перечислены выше и в contract.v1.json.
- **Текст не покидает раннер.** Текст, который вы передаёте на обработку, обрабатывается моделью вашего агента на условиях её собственной политики; humanizer-ru не добавляет ни одного нового получателя данных.
- **PyPI-пакет.** `humanizer-ru` содержит локальные скрипты без телеметрии и сетевых вызовов.

## Вопросы

Issues: <https://github.com/Vladimir-Human/humanizer-ru/issues>
