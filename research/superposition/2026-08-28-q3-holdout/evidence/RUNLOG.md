# Журнал прогонов Q3 (2026-08-28)

1. ~10:0x — фаза 1: генерация гипотез (6 исполнителей + арбитраж
   deepseek-v4-pro-0813; вывод первого арбитража усечён в трассе —
   повторный запуск арбитра на механическом дайджесте). След —
   `evidence/phase1/hypotheses-fan.txt`.
2. ~10:1x — `registry.md` (слоты заполнены; H0 сформулирован капитаном:
   арбитр зафиксировал пустой null-слот у исполнителей) и
   `preregistration.md` (правило коллапса + прогнозы P1–P5).
3. ~10:2x — заморозка v1: 60 файлов (24 ИИ + 26 человеческих + 2 boundary
   + манифест + 4 скрипта + marker-sources.json + реестр +
   предрегистрация + measure_q3.py v1).
4. ~10:3x — генерация пробы: 9 агентов (qwen3.8-max, ZHIPU/GLM-5.3,
   kimi/kimi-k3 × 3 промпта серии 1), сохранены дословно в
   `evidence/probe/`; заморозка пробы `probe-freeze.sha256` (9 хешей)
   ДО сканирования.
5. ~10:3x — **Прогон 1** measure_q3.py: баг kstar (искал максимальный k,
   а не минимальный: k*(12)=12, power занижена, N_req=None). Прочие секции
   корректны. Файл вывода перезаписан прогоном 2 (баг — в отчётной секции
   мощности; сверка секций: race/audit не зависят от kstar).
6. ~10:4x — fix kstar (range 1→n вместо n→1); ре-фриз measure_q3.py
   (v2 = 12866836…); corpora и предрегистрация не менялись.
7. ~10:4x — **Прогон 2** (канонический): k*(12)=10 (α=0.0193),
   power(12,0.7)=0.2528, N_req(0.7,80%)=37; CP-LB: 9/9=0.664,
   14/14=0.768, 19/19=0.824; регистр: классика 21/26 (80.8%);
   реестр-корпус: 4 семейства без поддержки; проба: max features=2,
   маркеров=1 (invisible_layout, kimi-02-pechat.txt:34).
8. ~10:5x — **Контроль (пост-правило)**: маркеры на серии 1 — 11
   срабатываний (gemini_span ×10, zero_width ×1). Артефакт:
   `evidence/baseline-markers-control.txt`. Мотив: срабатывание
   invisible_layout на пробе потребовало базлайн для интерпретации.
   Упущение капитана: условие «хотя бы одно срабатывание маркера A/B»
   не проверено на базлайне до заморозки правила; условие оказалось
   недискриминативным (срабатывает и на серии 1). Выносится в консилиум
   и decision честно, без переписывания правила задним числом.
9. ~11:0x — консилиум фазы 4 (kimi/kimi-k3 + ZHIPU/GLM-5.2, арбитр
   deepseek-v4-pro-0813) → `consilium.json`. Итог: буква = H2, замысел =
   H1, условие дефектно, дыра гарда подтверждена обоими; расхождение в
   диспозиции (законность интент-разрешения) → арбитр заблокировал
   коллапс.
10. ~11:1x — `decision.md`: КОЛЛАПС НЕ СОСТОЯЛСЯ (по протоколу:
    заблокированный консилиум + сработавшее условие стоит на
    подтверждённо ложноположительном замере). Путь разблокировки —
    три варианта на выбор владельца (правка гарда → перепрогон / буква
    H2 / замысел H1). Пост-якорь — `post-run-anchor.sha256`.
11. Гейт check_superposition.py доработан (18/18 selftest): пустой
    раздел «Отколлапсированные ветки» легитимен только при явном
    маркере отказа от коллапса (спек: decision = коллапс ИЛИ
    обоснованный отказ).
12. Пост-коллапсные правки (по прецеденту Q1, задокументированы здесь):
    `registry.md` — добавлен раздел «Исход цикла» (статусы; замерных
    данных правка не касается; preregistration.md НЕ менялся — хеш в
    заморозке совпадает); `RUNLOG.md` — этот раздел. Заморозка
    пере-якорена (v2): строка registry.md обновлена на текущий хеш.
    Пост-якорь — v1 (5 файлов).
13. Ревью фазы 5 (kimi/kimi-k3, «принята с правками»): вскрыто, что
    утверждение п.12 о пере-якорении registry.md было ЛОЖНО — regex
    замены ("q3-holdout\.registry\.md$" с точкой вместо слэша) не
    матчил строку, замена молча не применилась (тот же класс бага, что
    в Q1). Исправлено: ре-якорь по базовому имени (registry.md в
    заморозке = 7121791b…, проверено); пост-якорь расширен до 8 файлов
    (добавлены registry.md, decision.md, corpus-freeze.sha256). Прочие
    находки ревью: легенда статусов QUESTIONS.md дополнена;Minor/NIT
    (валидация условий на базлайне до заморозки — процессный урок Q4;
    хвост вывода маркер-скана в stats.json) — зафиксированы, не чинятся
    задним числом.

Наблюдения для консилиума:

- Проба: features 2 = максимум серии 1 (сдвига мягких сигналов нет);
  маркеров 1 < 11 серии 1 (ниже базлайна), но invisible_layout — тип,
  отсутствующий в серии 1; вероятная причина — дыра гарда: ⌨ U+2328
  (эмодзи-база вне диапазонов гарда U+1F000–U+1FAFF / U+2600–U+27BF)
  + VS16; кандидат в бэклог (правка маркера — полный конвейер AGENTS.md).
- Ревизорские семьи (Kimi, GLM) частично пересекаются с генераторами
  пробы (kimi-k3, GLM-5.3): ревизия механическая (срабатывание правил),
  не предпочтительная; оговорка фиксируется в consilium.json.

Замечание kimi-ревизора о фразе P2: «сдвиг (features >= 3 или маркер
A/B) — тогда H2 выигрывает» в предрегистрации содержит внутреннюю
неувязку с контекстом консервативного прогноза (прогноз предсказывает
отсутствие сдвига, при котором H2 проигрывает); читается как описка в
формулировке прогноза, правило коллапса однозначно и от прогноза не
зависит. Зафиксировано здесь, не правится задним числом.
