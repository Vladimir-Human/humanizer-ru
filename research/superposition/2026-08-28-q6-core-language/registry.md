# Q6: реестр гипотез — разделение ядра и языкового слоя

Дата: 2026-08-28. Вопрос из BACKLOG («Разделение ядра и языкового слоя,
чтобы скилл можно было перенести на другой язык»; Z1 заморожен).
Правило спека Q6: ветки предзаписаны; «реактивация только через канал
C1 (подпись по GOVERNANCE.md в REBALANCE); агент готовит анализ, не
код». Цикл замеров не проводится — вопрос закрыт аналитической
запиской (analysis.md) без изменения кода.

```yaml
- id: H0
  slot: null
  statement: "Не разделять, пока Z1 заморожен (внешний спрос не
    сработал); ядро уже разделено документально"
  status: collapsed_winner(анализ подтверждает: SKILL.md без лексики,
    классы A/B универсальны, пороги константы; рекомендация — не
    разделять до триггера C1)
- id: H1
  slot: mechanistic
  statement: "Вынести языконезависимое ядро (дерево, рамка честности,
    классы A/B) от русского слоя (паттерны #10–15n, морфология)"
  status: dormant(воскрешение: триггер C1 — подпись по GOVERNANCE.md;
    кандидат на вынос — REGISTRY scan_soft_signals, 582 строки
    кириллицы, в данные)
- id: H1b
  slot: mechanistic
  statement: "Перенос на другой язык унаследует ~3/4 маркеров
    автоматически (интерфейсные артефакты поставщиков
    языконезависимы); языковой блок — только REGISTRY сканера и словарь
    жанровых фраз"
  status: dormant(воскрешение: вместе с H1 — тот же триггер C1)
- id: H-INV
  slot: inversion
  statement: "Разделение раньше внешнего спроса — преждевременная
    абстракция: умножит поверхность гейтов без пользователей"
  status: alive(подтверждена анализом: спрос заморожен, стоимость
    сопоставима с релизом, ядро уже отделено документально)
- id: H-OOF
  slot: out-of-frame
  statement: "Вопрос не «разделять ли», а «кто спрашивает»: без
    внешнего пользователя мультиязыка разделение — работа на склад;
    правильный вопрос — чтo минимизировать сейчас (ответ анализа:
    ничего, документальная разделённость достаточна)"
  status: alive(рамка)
```

Слоты: null (H0), mechanistic ×2 (H1, H1b), inversion (H-INV),
out-of-frame (H-OOF). Все четыре слота заполнены.
