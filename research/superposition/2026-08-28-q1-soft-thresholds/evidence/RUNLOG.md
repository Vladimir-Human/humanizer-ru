# Журнал прогонов Q1 (2026-08-28)

Хронология по mtime файлов (см. post-run-anchor.sha256):

1. 07:43 — `registry.md` записан (фаза 1: параллельное делегирование 6
   исполнителям + арбитр;
   сырой след — `evidence/phase1/hypotheses-fan.txt`).
2. 07:44 — `preregistration.md` записан: прогнозы P1–P10 + правило коллапса
   ДО новых замеров. Хеш включён в заморозку v1.
3. ~07:46 — `evidence/corpus-freeze.sha256` (v1): 60 хешей — 24 ИИ + 26
   человеческих + 2 boundary файла, манифест, оба скрипта сканера/свипа,
   оба приорных JSON, measure.py v1, registry.md, preregistration.md.
4. 07:48 — **Прогон 1** (pwsh, measure.py v1): все секции посчитаны; в
   integrity-секции 100 ложных расхождений — баг сравнения путей в
   КОМПАРАТОРЕ отчёта (свежие абсолютные пути против приорных
   относительных). Замеренные значения (race, criticality, histogram, AUC)
   от сравнения не зависят. Вывод сохранён как
   `stats-run1-integrity-bug.json` (переименование, mtime сохранён).
5. ~07:49 — fix: integrity_check нормализует пути к относительным.
   Единственная правка measure.py; касалась только отчётной секции P1.
6. 07:50 — re-freeze (v2): в corpus-freeze.sha256 заменена строка хеша
   measure.py (v2 = cb552bc2…); хеши корпусов и предрегистрации НЕ
   менялись (проверяемо: все прочие 59 строк идентичны v1).
7. 07:51 — **Прогон 2** (канонический): `stats.json`; integrity = 0
   расхождений; все прочие секции байт-в-байт совпадают с прогоном 1
   (детерминизм; сверено по секциям при повторном просмотре).
8. ~08:0x — консилиум фазы 4 (2 семьи + арбитр) → `consilium.json`.
9. После — пост-якорь `post-run-anchor.sha256` (хеши выводов).

Замечание ревизора Qwen о «прогоне до заморозки» относится к пункту 4/6:
прогон 1 выполнен при заморозке v1 (корпуса+правило), но до v2 (хеш
починенного measure.py). Правило коллапса и корпуса между прогонами не
менялись (хеши неизменны). Для будущих циклов принято: прогон до заморозки
запрещён; при починке скрипта — новый прогон целиком и запись здесь.

Рекомендации консилиума, принятые в работу:
- счётчик покрытия в integrity (сравнено N файлов, а не пустой список) —
  backlog: мера следующего цикла, замороженный measure.py этого цикла не
  правится;
- git-якорь: каталог коммитится этим же changeset'ом;
- пост-якорь хешей выводов — post-run-anchor.sha256 (этот каталог);
- фальшификаторы H-S2/H-S3 согласовать с прогнозами — backlog
  (прогнозы опровергнуты данными независимо от дефекта формулировок).

## Пост-коллапсные правки (ревью фазы 5, ~08:25–08:4x)

Файлы реестра правились ПОСЛЕ замеров и пост-якоря v1; причины и состав:

1. `registry.md` (~08:25): (а) статусы гипотез обновлены по decision.md —
   H0/H-C/H-R/H-D → dormant с условиями воскрешения, H-T → collapsed_winner,
   добавлен сводный абзац статусов; (б) нейтрализация формулировок по
   check_outward (жаргон параллельного прогона и атрибуция подписи
   заменены нейтральными); (в) исправлен путь фазы 1
   (evidence/phase1/hypotheses-fan.txt). Замерных данных правки не касались;
   пре-коллапсные байты в git-истории (коммит 96f60e6 содержит
   пре-якорную версию, снапшот до правки статусов невосстановим —
   правки выполнялись в рабочем дереве до коммита).
2. `RUNLOG.md`: документирование этого раздела.
3. `consilium.json`: нейтрализация формулировки сырой записи (внутренний
   жаргон параллельного прогона удалён) — ревизия записи, не содержания
   вердиктов.
4. `stats-run1-integrity-bug.json`: из путей удалён локальный префикс
   (файл создан прогоном с абсолютными путями; содержание замеров не
   менялось — см. stats.json, где пути относительные).
5. `evidence/corpus-freeze.sha256` и `evidence/post-run-anchor.sha256`:
   пере-якорение v2/v3/v4 — строки registry.md/RUNLOG.md/consilium.json/
   stats-run1-integrity-bug.json обновлены на текущие хеши после каждой
   правки из этого списка.

Пере-якорение v4 фиксирует текущее состояние; правило «правило старше
данных» проверяется гейтом по mtime preregistration.md против замерных
файлов evidence/ (preregistration.md не правился — хеш в заморозке v1
должен совпасть; сверка: см. ниже).

## Ревью фазы 5 и post-hoc дополнение (вторая половина дня)

13. Ревью фазы 5 (kimi/kimi-k3): «принята с правками» — 3 major
    исправлены коммитом 096e16f (якоря пере-верифицированы, абсолютные
    пути вычищены, гейт усилен до 4 слотов с selftest 18/18).
14. Подпись по GOVERNANCE.md получена (владелец передал выбор
    конфигурации исполнителю в явной форме). На этапе внедрения — до
    любой правки SKILL.md — выполнен контроль, отсутствовавший в гонке:
    скан adversarial-FP корпуса (12 файлов, жанры манифеста):
    T=2/K=1 → FP=2 (06-blog-programmista-markdown-emoji-title-case.txt,
    11-statya-pro-priznaki-ii-kavychechnaya-lovushka.txt),
    T=2/K=2 → FP=1 (11-statya), T=3 → FP=0; boundary-контроли чисты.
    Сработало условие воскрешения H0, записанное до замера в решении → повторное
    применение правила коллапса с расширенным блокирующим гейтом
    (26 human + 12 adversarial + 2 boundary) → коллапс в H0
    (статус-кво T=3/K=2 подтверждено). Артефакт:
    `adversarial-fp-posthoc.json`; дополнение — в decision.md.
    SKILL.md не менялся; внедрение T=2/K=1 отменено данными.
    Урок — в BACKLOG: все FP-контроли репозитория входят в блокирующий
    гейт будущих гонок по умолчанию.
