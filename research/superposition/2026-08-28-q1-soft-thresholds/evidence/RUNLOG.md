# Журнал прогонов Q1 (2026-08-28)

Хронология по mtime файлов (см. post-run-anchor.sha256):

1. 07:43 — `registry.md` записан (фаза 1: параллельное делегирование 6
   исполнителям + арбитр;
   сырой след — `evidence/phase1/hypotheses-fan.txt`).
2. 07:44 — `preregistration.md` записан: прогнозы P1–P10 + правило коллапса
   ДО новых замеров. Хеш включён в заморозку v1.
3. ~07:46 — `evidence/corpus-freeze.sha256` (v1): 60 хешей — 24 ИИ + 26
   человеческих + 2 boundary файла, манифест, оба скрипта сканера/свипа,
   оба приорных JSON, measure.py v1, registry.md, preregistration.md.
4. 07:48 — **Прогон 1** (pwsh, measure.py v1): все секции посчитаны; в
   integrity-секции 100 ложных расхождений — баг сравнения путей в
   КОМПАРАТОРЕ отчёта (свежие абсолютные пути против приорных
   относительных). Замеренные значения (race, criticality, histogram, AUC)
   от сравнения не зависят. Вывод сохранён как
   `stats-run1-integrity-bug.json` (переименование, mtime сохранён).
5. ~07:49 — fix: integrity_check нормализует пути к относительным.
   Единственная правка measure.py; касалась только отчётной секции P1.
6. 07:50 — re-freeze (v2): в corpus-freeze.sha256 заменена строка хеша
   measure.py (v2 = cb552bc2…); хеши корпусов и предрегистрации НЕ
   менялись (проверяемо: все прочие 59 строк идентичны v1).
7. 07:51 — **Прогон 2** (канонический): `stats.json`; integrity = 0
   расхождений; все прочие секции байт-в-байт совпадают с прогоном 1
   (детерминизм; сверено по секциям при повторном просмотре).
8. ~08:0x — консилиум фазы 4 (2 семьи + арбитр) → `consilium.json`.
9. После — пост-якорь `post-run-anchor.sha256` (хеши выводов).

Замечание ревизора Qwen о «прогоне до заморозки» относится к пункту 4/6:
прогон 1 выполнен при заморозке v1 (корпуса+правило), но до v2 (хеш
починенного measure.py). Правило коллапса и корпуса между прогонами не
менялись (хеши неизменны). Для будущих циклов принято: прогон до заморозки
запрещён; при починке скрипта — новый прогон целиком и запись здесь.

Рекомендации консилиума, принятые в работу:
- счётчик покрытия в integrity (сравнено N файлов, а не пустой список) —
  backlog: мера следующего цикла, замороженный measure.py этого цикла не
  правится;
- git-якорь: каталог коммитится этим же changeset'ом;
- пост-якорь хешей выводов — post-run-anchor.sha256 (этот каталог);
- фальшификаторы H-S2/H-S3 согласовать с прогнозами — backlog
  (прогнозы опровергнуты данными независимо от дефекта формулировок).
