workflow "q2-hypotheses" completed (7 agents).
Return value:
{
  "results": [
    {
      "id": "hyp-product",
      "model": "deepseek-v4-flash-0731",
      "ok": true,
      "seconds": 234,
      "result": "# Ответ: lint-only как продуктовая линия — механически различающиеся варианты\n\n**База фактов, на которую опираются все предсказания** (корпус G5: 14 артефакт-несущих, 5 чистых, 11 human; гейты D: 0 остаточных на 14, чистые байт-в-байт не тронуты, human 11/11 не тронуты, meaning_loss 0; D>A = 13/1/5, B>D = 14/14, p=0.000122). Ключевое механическое различие вариантов — **чем гарантируется ценность**: вероятностной (предпочтение панели), точной (байтовые инварианты), или эквивалентностью (неотличимость от уже существующего).\n\n---\n\n## (а) Отдельный режим скилла: «просто почисти артефакты»\n\n- **Что продаёт:** снятие машинных артефактов *без переписывания* — «удали копипаст-мусор, стиль не трогай». Обещание безопасности, а не качества.\n- **Сегмент:** тексты, где перезапись запрещена жанром/условием — договоры (уже есть в дереве решений SKILL.md: «только удалить артефакты класса A»), академ-сабмишены, тексты до 11/2022, файлы-контейнеры.\n- **Предсказание:** на 14 артефакт-файлах G5 с инструкцией «только удали артефакты» lint-only должен повторить D>A: **≥ 11/14 вин против исходника** при 0/14 остаточных маркеров и 0/11 тронутых human.\n- **Фальшификатор:** ≤ 8/14 вин на той же подгруппе; либо meaning_loss > 0 на чистой ветке; либо B (полная перезапись) предпочтён даже при явном запрете. Плюс унаследованный бэкграунд: G6 отклонил расщепление режимов 19/19 — новый «режим» несёт бремя опровержения этого.\n- **Цена ошибки:** включить без ценности — два режима-близнеца, выбор режима падает на пользователя (плодит интерфейс, противоречит TERMINAL-narrowed-product.md: единый поток). Не включить при ценности — сегмент «нельзя переписывать» остаётся на нестабильном решении агента «когда остановиться».\n- **Обратимость:** высокая — текстовая секция в SKILL.md + `references/` (в двух синхронных копиях), счётчики в README.\n- **Гейты:** `check_bundle_sync` (двойная копия текстов), `check_docs` (витринные числа), `check_examples` (если добавится пара «До/После»). Код не трогается вообще.\n- **Механический признак:** гарантия вероятностная — мера это доля предпочтений панели на ограниченной подгруппе, нового корпусного замера не существует, его надо создавать.\n\n## (б) Расширение CI-экшена до фикса\n\n- **Что продаёт:** «не только лови, но и чини» — `action/action.yml` получает режим автофикса (`filemarks.py --clean` как шаг вместо `--scan`), артефакты класса A снимаются прямо в репозитории.\n- **Сегмент:** владельцы репозиториев, у которых в контенте копипаст из чат-ботов; тот же «инструмент для своих», но с мутирующей силой.\n- **Предсказание:** пакетный прогон fix-режима по корпусу G5 обязан воспроизвести гейты D дословно: **0/14 остаточных маркеров, 0/5 байт-изменений в чистых, 0/11 изменений в human**. Это уже измерено для скрипта — остаётся измерить для шага action'а в чужом репо.\n- **Фальшификатор:** любая единица из трёх инвариантов: 1 изменённый чистыЙ файл или 1 тронутый human-файл убивают автофикс навсегда — CI не отличает human-файл от AI-файла, а мутация вне детерминированных гейтов недопустима. Слабое место: попытка фиксить мягкие признаки (они стилистические, детерминированно не снимаются).\n- **Цена ошибки:** включить при нарушении инвариантов — сожжено доверие к ныне безопасному read-only гейту; не включить — линия «фикс за один шаг» не существует, хотя на файлах-контейнерах (PNG/DOCX-метаданные) фикс — единственный доступный путь (перезапись туда не ходит), потеря реальна.\n- **Обратимость:** средняя — новый инпут в `action.yml`, точка входа скрипта, зеркало `src/humanizer_ru/`, README/CHANGELOG; откат = revert.\n- **Гейты:** `check_pkg_sync` (зеркало скриптов), `check_all` (58 гейтов), обязательный `--selftest` у нового режима, `check_removal_parity` (снятие↔детектор — уже стоит, это и есть страховка).\n- **Механический признак:** гарантия точная, байтовая — мера это три счётчика инвариантов на корпусе, а не предпочтение.\n\n## (в) Lint остаётся внутренним шагом перезаписи (препроцессинг)\n\n- **Что продаёт:** ничего отдельно — это текущая позиция TERMINAL-narrowed-product.md: «единый поток перезаписи + детерминированный lint-слой». Обещание отсутствия выбора у пользователя.\n- **Сегмент:** все; S3 уже отклонён данными (единый поток 19/19).\n- **Предсказание:** сравнение B (перезапись) против B′ (преклининг + перезапись) на 14 файлах G5: если **≥ 12/14 выходов байт-в-байт совпадают** — lint как препроцессинг избыточен (он лишь гейт); если B′ снижает остаточные маркеры или разброс судей — слой окупается.\n- **Фальшификатор:** B′ ≡ B на всех 14 файлах — «пользы препроцессинга нет», lint доказуемо не продукт, а внутренняя гигиена. Это единственный вариант, чей фальшификатор — *равенство*, а не превосходство.\n- **Цена ошибки:** оставить внутренним без замера — вечная неопределённость вклада слоя (шаг «скрытый holdout» из TERMINAL не изолирует lint); вынести наружу без данных — повторение ошибки S3 (расщепление, которое G6 уже отклонил).\n- **Обратимость:** высочайшая — ноль продуктовой поверхности; только внутренняя архитектура.\n- **Гейты:** новых нет; держится на `check_removal_parity` (паритет снятие↔детектор после дефекта I.8).\n- **Механический признак:** ценность не обещана, а *измеряется как дельта* B′−B; при нулевой дельте решение принимается само.\n\n## (г) Позиционирование «инструмент для своих репозиториев» vs «сервис для текстов»\n\n- **Что продаёт (инструмент):** гейт/фикс, чья ценность — *инварианты*: «твой репо не течёт артефактами». Что продаёт (сервис): *предпочтение*: «дай текст — получи текст, который люди выбирают».\n- **Сегмент:** инструмент — разработчики, CI-интеграция (action уже опубликован, детекция); сервис — авторы/редакторы, web-демо (vladimir-human.github.io/humanizer-ru) и скилл.\n- **Предсказание:** два *разных* замера. Инструмент: те же **0/5 + 0/11 + 0/14** на корпусе — то, что покупает владелец репо (уже измерено в G5, доказуемо в чужом CI). Сервис: **человеческая** (не модельная) панель предпочитает вывод скилла на подмножестве пар — это шаг 3 плана TERMINAL, ещё не выполнен; модельные 14/14 и 9/9 — лишь суррогаты.\n- **Фальшификатор:** инструмент — любой сработавший human-канарейки (ложные срабатывания на 11/11 — смерть); сервис — человеческая панель не воспроизводит модельное предпочтение либо находит голосовые повреждения (у контрфактов 2/6 против 0 у скилла — порог известен).\n- **Цена ошибки:** продавать как сервис то, что покупают как инварианты (и наоборот) — рассинхрон обещания и доказательства: публика видит разночтение (About, как предупреждает AGENTS.md, гейтами не сверяется); не выбрать вообще — маркетинг определяют данные, а не наоборот.\n- **Обратимость:** текстовая — README/About/метаданные скилла; но репутационно медленная (About публичное и не гейтится).\n- **Гейты:** ручное обновление About (не входит в `check_docs`); сам выбор позиционирования гейтами не проверяется.\n- **Механический признак:** различие не в замере качества, а в том, *какой замер покупатель держит в руках* — байтовые инварианты (инструмент) против человеческого предпочтения (сервис).\n\n---\n\n**Сводка различий одной строкой:** (а) = доля предпочтений при запрете перезаписи (новый замер); (б) = три байтовых счётчика автофикса (замер уже сделан для скрипта, осталось для action); (в) = дельта B′−B, где фальсификатор — идентичность, а не победа; (г) = выбор носителя доказательства (инварианты vs человеческая панель). Общий ограничитель всех вариантов: результат B>D 14/14 остаётся главным фальсификатором любой претензии, что lint-only *достаточен* как продукт — lint легитимен только как слой, гейт или сегментная линия с явным запретом перезаписи.\n\nКлючевые источники фактов: `research/agent/G5-exp1-report.md`, `research/agent/HYPOTHESES.md` (S2 = evidence_pending), `research/agent/TERMINAL-narrowed-product.md` (текущий режим = единый поток), `action/action.yml` (только детекция), `scripts/filemarks/filemarks.py` + `scripts/check_removal_parity.py` (слой снятия и его паритет с детектором), `SKILL.md` (lint отдельной точкой входа не выделен)."
    },
    {
      "id": "hyp-segment",
      "model": "deepseek-v4-flash-0731",
      "ok": true,
      "seconds": 115,
      "result": "Срез готов. Все цифры — из предоставленного контекста; инструментарий (маркеры A/B, `filemarks --clean`, фичи) уже существует, так что сегментация измерима прямой переписью 33 файлов (≈0 затрат) — прогнозы ниже это априоры до переписи.\n\n**Оси (измеримые):**\n- **M** — A/B-маркеры после гард-фильтра: M+ = 3/33 (9%), все в старой партии 24 (gemini ×2, le-chat ×1; FP из пробы отбрасывается).\n- **S** — features_total ≥ порога действия: при кандидате T=2/K=1 — 7/24 → экстраполяция ~9–10/33 (гомогенность семейств — допущение; статистика S на пробе 9 новых семейств неизвестна). При подписанном T=3+ ось S вырождена (max=2 < 3).\n- Единственный свободный параметр модели — **overlap** M+∩S+ (0..3). Все 4 прогноза — функции от него.\n\n**1. «Только маркеры» (M+, S−)** → продукт = lint как автономный сервис (`filemarks --clean` решает механически).\n- Предсказание: **~2** (0–3, = 3 − overlap).\n- Фальсификатор: перепись даёт overlap ≥2 (маркерные файлы системно выше порога); либо post-lint файл сегмента всё ещё ≥T.\n- Цена ЛП (это на деле «оба»): клиент получает текст, всё ещё звучащий ИИ → возврат, смерть обещания «чисто». Цена ЛО (гоняем на ручную правку): теряем маржу дешёвого механического кейса, pipeline медленнее.\n\n**2. «Только мягкий» (M−, S+)** → продукт = переписывание; lint бесполезен (снимать нечего).\n- Предсказание: **~9** (7–10, = 9.6 − overlap).\n- Фальсификатор: любое A/B-срабатывание в сегменте (неполнота маркерного набора → это «оба»); или подписан порог 3+ → сегмент пуст.\n- Цена ЛП (маркер там есть, а мы шлём на правку): правка дороже и медленнее «lint+добивка». Цена ЛО (классифицируем как «ничего»): ИИ-текст проходит гейт — главный клиентский сценарий провален.\n\n**3. «Оба» (M+, S+)** → продукт = конвейер: lint как препроцессинг + правка мягкого слоя.\n- Предсказание: **~1** (0–3, = 3 × (7/24) при независимости).\n- Фальсификатор: overlap = 0 на переписи → сегмент пуст, двухступенчатый конвейер — оверинжиниринг; новый или системный M+=0 у новых семейств → сегмент остаётся только у старожилов.\n- Цена ЛП (конвейер при пустом сегменте): лишняя сложность, дороже и медленнее для 2/3 корпуса. Цена ЛО (лечим как «только маркеры»): после lint остаётся мягкий слой → дефект продукта на реальном сегменте.\n\n**4. «Ничего» (M−, S−)** → не клиент (или бесплатный вердикт «текст уже чист»).\n- Предсказание: **~21** (20–21, = 33 − 3 − 9.6 + overlap).\n- Фальсификатор: любой файл сегмента срабатывает при финальных настройках; либо клиентские возвраты показывают «звучит ИИ» при M−S− — инструменты слепы (это провал валидности, не измеримости).\n- Цена ЛП (объявляем не-клиентом при живом сигнале): потерянный доход + дыра детектора под внешний аудит. Цена ЛО (тащим в обработку): тратим lint/правку на файлы без сигнала — маржа в минус; но дешевле ЛП → асимметрия в сторону «ошибиться в наличие сигнала».\n\n**Продуктовые гипотезы по семьям:** «только маркеры» = лёгкие выводы gemini/le-chat (их артефакты детерминированные — лечатся механикой); «только мягкий» = новые семейства qwen3.8-max/GLM-5.3/kimi-k3 (в пробе 9 — 0 настоящих маркеров), их дорожка «правка»; «оба» = тяжёлые выводы старожилов (features ≥2); «ничего» — короткие/чистые выводы, и это ровно та клетка, где лежит человеческий корпус (26 файлов, M=0/S=0) → «не клиент» инструментально неотличим от «человек».\n\n**Райдеры:** (1) вся сегментация измерима только при принятии T=2/K=1 — при подписанном 3+ сегменты 2–3 пусты, а модель вырождается в «маркеры vs не клиент»; (2) первое измерение — soft-статистика на 9-файловой пробе: она двигает все ожидания по сегменту 2; (3) n=33 и overlap оценивается по 3 маркерным файлам — верить только полной переписи, не точке прогноза."
    },
    {
      "id": "hyp-safety",
      "model": "deepseek-v4-flash-0731",
      "ok": true,
      "seconds": 519.3,
      "result": "**Замер (база для всех предсказаний).** Прогнал filemarks-эквивалентный текстовый pipeline (`clean_text_layer` → `clean_markup`, тот же путь, что `filemarks.py --clean` для `kind=text`) по 26 human + 2 boundary:\n\n- **28/28 файлов байт-в-байт нетронуты (n=0 всюду).** Контроль: `check_corpus.py` — human 0 совпадений, boundary совпал; `filemarks --selftest` — 43/43 PASS.\n- «Легальные невидимые» в корпусе и нетронуты: Перельман (15) — NBSP ×18, наборный пробел U+2000–200A ×1, узкий NBSP ×1; it-notation (11) — ZWJ ×2 **внутри 👨👩👧**; boundary emoji-zwj — ZWJ ×4 + VS16 ×1 стоят.\n- Зонды «живых» текстов, где pipeline **меняет байты**: мягкий перенос U+00AD (n=2), BOM U+FEFF (n=1), `<thinking>…</thinking>` в середине строки (удалён **с содержимым**, детектор молчит), `:::note{title=\"Совет\"}` (удалён, детектор молчит), «в копиях бывает oai_citation:5‡ и turn0search0 и [cite: 3]» (удалены все три), «Python+3 и Java+2» (снято, детектор молчит), иконки PUA U+E200–E204 (сняты). Контейнеры/картинки: MD frontmatter `model: claude-4` снят (`author` сохранён); PNG — **все** tEXt (Author/Copyright/Software) сняты (`strip_all_text=True` по умолчанию) + exiftool.\n\n---\n\n**(а) На каких живых текстах lint легально меняет байты**\n- **Предсказание:** на 26+2 — 0 байт (измерено). На живых текстах меняет на 8+ классах: SHY U+00AD (оцифрованная классика с переносами), BOM (файлы из-под Windows-блокнота), VS16 вне эмодзи-контекста, PUA-иконки E200–E204, `<thinking>`-примеры в обучающих текстах, `:::note{…}`-адмоншены, текст-о-маркерах (снимется даже в кавычках), цепочки «X+N и Y+N». **Научная нотация не снимается** (U+202F и U+2000–200A — в корпусе есть, не тронуты).\n- **Фальшификатор:** добавить в корпус файл с U+00AD / BOM / PUA-иконкой → n>0 и байты меняются; текущий 26+2 такого не содержит.\n- **Цена ошибки:** FP (снял легальный символ) — порча живого текста, у `<thinking>`-примера удаляется контент целиком; FN — машинный текст остаётся помеченным, низкая, по Главному правилу приемлема.\n\n**(б) Boundary-файлы (emoji ZWJ)**\n- **Предсказание:** 0 байт на обоих (измерено; ZWJ ×4 + VS16 ×1 в boundary и ZWJ ×2 внутри 👨👩👧 в корпусе стоят — guard lookbehind/lookahead эмодзи-диапазонов работает).\n- **Фальшификаторы:** ① ZWJ между буквами — снимается; ② эмодзи за пределами guard-диапазонов U+1F000–1FAFF / U+2600–27BF / U+1F1E6–1F1FF (новый блок эмодзи ломает guard); ③ **keycap** «цифра+VS16+U+20E3» — VS16 съедается (измерено: n=1, «1️⃣» ломается); ④ текстовый селектор U+FE0E (измерено: снимается).\n- **Цена ошибки:** FP — сломанный рендер составных эмодзи в подписях/живых текстах; FN — ничего (эмодзи не маркер — правильно пропускать).\n\n**(в) Cнимает ли lint намеренно расставленное человеком**\n- **Предсказание:** на 26+2 — 0 (корпус таких элементов не содержит по построению). На живых текстах — **да, по дизайну**: frontmatter `model:/llm:/generator:/provenance` (измерено: `model` снят), **все** tEXt/EXIF-метаданные PNG/JPEG (измерено: Author/Copyright/Software сняты), описание артефактов в кавычках (измерено), PUA-иконки.\n- **Фальшификатор:** добавить в корпус .md с `model:` или .png с tEXt Author → container/image-путь меняет байты (зонды подтверждают).\n- **Цена ошибки:** FP — уничтожение намеренной атрибуции/провенанса/лицензионных меток в чужих ассетах (высокая, юридически значимая); FN — остаётся «мусор», который автор сознательно оставил (низкая).\n\n**(г) Явный запрос vs автолинт в CI чужого репо**\n- **Предсказание:** на 26+2 оба режима **неразличимы — 0/28 байт** (корпус чист; CI-сценарий без живых элементов даст тот же 0). Чистота корпуса — **не свидетельство** безопасности lint вне корпуса.\n- **Отличия:** явный — вход не трогается по умолчанию (`-o` → `<stem>.cleaned<ext>`), оператор видит диф, известны жанр и намерение, есть апелляция. CI чужого репо — без человека: переписывает каждый файл (атомарная запись безусловна — mtime меняется даже при n=0), режет метаданные всех PNG/JPEG (хэши/подписи/лицензии), удаляет frontmatter-провенанс, режет `<thinking>`/`:::note`/«+N» — **cleaner шире детектора** (think_tag, writing_block, source_plus_chain) и **без backtick-guard** (`_inside_backticks` есть у детектора, у снятия — нет: в README репо пример `turn0search0` в обратных кавычках будет удалён).\n- **Фальшификатор:** на 26+2 — нет (оба 0); на живом CI-репо: любой .md с `model:` / PNG с tEXt Author → байты меняются автоматически (подтверждено зондами).\n- **Цена ошибки:** FP в CI — молчаливая, часто необратимая порча чужого контента и ассетов и их истории (коммиты автолинта, диф на десятки файлов ревьюер не отследит); FN — ложная «зелёность» CI при оставшейся метке (дешёвая, но подрывает доверие к гейту как гарантии).\n\n**Вывод среза.** Контракт «не тронуто ни байта» lint выполняет ровно на своём корпусе (0/28, подтверждено). Вне корпуса он легально меняет байты живых текстов минимум на 8 классах (SHY, BOM, VS16/keycap, PUA-иконки, думающие-теги, адмоншены, текст-о-маркерах, провенанс-метаданные и все tEXt/EXIF PNG/JPEG). Boundary-guard держит только тот класс эмодзи, под который спроектирован; дыры — keycap и эмодзи вне рейнджей. Для выделения lint в самостоятельный продукт условие «на корпусе 0» — минимальный контракт, но оно не защищает чужой живой текст: различие явный/CI по байтам на корпусе неуловимо, а добавление контекстного слоя (жанр, диф, согласие) — единственное, что держит асимметрию цены ошибки в сторону «пропустить, а не испортить»."
    },
    {
      "id": "hyp-economics",
      "model": "deepseek-v4-flash-0731",
      "ok": true,
      "seconds": 91.5,
      "result": "Всё по делу — сразу к списку. Ниже каждая гипотеза в одном блоке: механизм / предсказание (формат замера) / фальсификатор / цена ошибки в обе стороны / обратимость.\n\n---\n\n**(а) Выделение lint каннибализирует перезапись**\n\n- **Механизм:** у lint есть пороговое удовлетворение («текст больше не пахнет ИИ») при ~нулевой цене и усилии; у перезаписи — цена вовлечённости (прочитать правки, принять изменения). Пользователь с ограниченной готовностью платить останавливается на lint. Вместо лестницы lint→перезапись получается замена.\n- **Предсказание (формат):** телеметрия сессий — доля lint-only; конверсия lint→перезапись за 7 дней; когортный LTV по точке входа (lint-первый vs перезапись-первая). Различающий замер — A/B: lint как first-class режим vs lint, доступный только изнутри потока перезаписи; метрика — объём перезаписи на юзера.\n- **Фальсификатор:** после запуска отдельного lint объём/частота перезаписи не падает; lint-пользователи инкрементальны (без lint не пришли бы совсем); lint→перезапись ≥ прямой ретеншн перезаписи.\n- **Цена ошибки:** поверили-неверно → спрятали lint, потеряли сегмент гигиены, сузили верх воронки; не поверили-а-верно → запустили lint, LTV на юзера падает, наружу уходит текст уровня 13/1/5 вместо 14/14 — репутация «хуманизатора» и подрыв тезиса S4.\n- **Обратимость:** высокая — это UI/дистрибуция: убрать/за пейволл/лимит n-раз; телеметрия ловит эффект до необратимых маркетинговых вложений.\n\n**(б) Lint и перезапись — разные сегменты спроса**\n\n- **Механизм:** гетерогенные job-to-be-done: «проверить / пройти гейт» против «улучшить текст». Выбор определяется задачей, а не дельтой качества; чувствительность к цене и усилию разная.\n- **Предсказание (формат):** кластеризация сессий по фичам (тип документа, длина, домен, источник: руками vs CI) — режимы должны разойтись по признакам; корреляция lint с дедлайнами/CI против перезаписи с редакционными циклами; stress-тест: lint-only не уходят при пейволле перезаписи.\n- **Фальсификатор:** одни и те же юзеры чередуют режимы на похожих текстах; lint-only принимают бесплатную перезапись («бесплатно — бери больше») — рынок один, выбирают ценой. (Высокая lint→перезапись — это уже не (б), а (в).)\n- **Цена ошибки:** поверили-неверно → навязываем/бандлим перезапись, churn сегмента гигиены, либо убиваем lint как «каннибала», теряя целый сегмент; не поверили-а-верно → проектируем один рынок (pricing/позиционирование) и режем оба.\n- **Обратимость:** высокая; проверяется ценовыми/позиционными экспериментами до обязательств.\n\n**(в) Lint как бесплатный верх воронки ведёт к перезаписи**\n\n- **Механизм:** lint = дешёвый первый контакт; доверие к суждению инструмента (панели уже показали: lint улучшает текст); вскрытый разрыв («всё ещё пахнет ИИ») + CTA → следующий шаг — полная перезапись.\n- **Предсказание (формат):** конверсия lint→перезапись (внутри сессии / 7 дней) заметно > 0; когортный LTV у входа через lint выше, чем у прямого входа (индукция); A/B «CTA в отчёте lint есть/нет».\n- **Фальсификатор:** конверсия ≈ 0 или не выше базовой; после отчёта юзер уходит, оценив текст как «достаточно» (это и есть победа (а)-порога); CTA не двигает конверсию.\n- **Цена ошибки:** поверили-неверно → инвестируем в lint, который конвертит будущих перезапись-пользователей в дешёвый исход — платим за собственный каннибализм; не поверили-а-верно → придушили канал привлечения, падает общий LTV, (б)-сегмент не находит продукт.\n- **Обратимость:** высокая — это про размещение CTA, тривиально откатывается и инструментируется сразу.\n\n**(г) Lint в CI — вообще другой рынок**\n\n- **Механизм:** пользователь CI — не автор, а владелец пайплайна (генерация доков, локализация, SEO-батч). Ему нужен детерминированный pass/fail-гейт, а не интерактивная редактура; перезапись human-in-the-loop семантически несовместима с CI. Другой покупатель, бюджет, критерии → кросс-эластичности нет.\n- **Предсказание (формат):** инсталлы action не коррелируют с использованием скилла; % CI-пользователей, когда-либо открывавших скилл, ≈ 0; из CI нет запросов на интерактивную перезапись; выручка по каналам не пересекается.\n- **Фальсификатор:** одни аккаунты в обоих местах; из CI прилетают запросы на перезапись прямо в пайплайне (rewrite-as-API) — рынки схлопываются в один «обработку контента», и CI-lint становится дешёвым субститутом перезаписи-API.\n- **Цена ошибки:** поверили-неверно → независимые pricing/позиционирование случайно создают арбитраж (CI-lint вместо перезаписи, дешевле); не поверили-а-верно → насильственная связка со скиллом тормозит принятие CI, либо недоинвестируем в CI, упуская пляцдарм для перезаписи-API.\n- **Обратимость:** высокая (action — аддитивная, версионируется); обратное слияние в один продукт дороже.\n\n---\n\n**Что НЕ измеримо на существующих корпусах — честно: все четыре.**\n\n- G5-корпус (14/14, 13/1/5) измеряет **порядок качества**, а не **поведение выбора**. Субституция, конверсия, сегменты, эластичность — поведенческие данные, их в корпусе нет. Гипотезы (а)–(г) все про спрос, а не про качество.\n- Из корпуса извлекаемо ровно одно: предпосылка (а) «юзер получит худшее качество» подтверждена (14/14), и корпус уже фальсифицирует вариант «lint бесполезен» (13/1/5). Шаг «юзер выберет дешёвое» — уже вне корпуса.\n- (а)/(б)/(в) требуют продуктовой телеметрии (сессии, конверсии, когорты, ценовой тест) — внешней к корпусу, но внутренней к продукту: измеримо сразу после запуска с инструментовкой; G5 тут только контекст.\n- (г) требует **внешних данных экосистемы**: инсталлы/зависимые GitHub action, фиче-реквесты от CI-пользователей, готовность платить за API. Не извлекается ни из корпуса, ни из телеметрии скилла.\n- Важно: один и тот же инструментарий (конверсия lint→перезапись + LTV по точке входа + кластеризация текстов + ценовой тест) различает (а) vs (б) vs (в) — это один замер с тремя прочтениями; (г) ортогонален и меряется отдельно."
    },
    {
      "id": "hyp-integration",
      "model": "deepseek-v4-flash-0731",
      "ok": true,
      "seconds": 226.9,
      "result": "# Срез: интеграция lint-only («только снятие артефактов») в humanizer-ru\n\n**База для всех вариантов.** Уже существует текстовая ветка-прецедент: «договор / нормативный акт → только удалить артефакты класса A; стилистику не применять». Lint-only — это обобщение этой ветки на любой жанр по явной просьбе. Снятие меток — уже открытая возможность продукта (README, removal-matrix), так что рамка «не портить живое, не дописывать факты, без вердиктов об авторстве» не меняется — меняется только точка входа.\n\n---\n\n## (а) Ветка дерева решений «запрос только на снятие артефактов» (текстовая, без скриптов)\n\n- **Механика.** SKILL.md: новый шаг дерева перед жанровым разбором — «просьба только вычистить маркеры („убери мусорные следы“, „вычисти копипаст из чата“)? → режим CLEAN»: прогнать только класс A по `chatbot-artifacts.md` (текст или Grep готовыми подстроками), снять маркеры, восстановить ссылки, стилистику не трогать, повторный прогон, отчёт «снято N, вне покрытия M». Справочник: секция в `rewrite-guide.md` (или `references/clean-mode.md`) с процедурой; можно переиспользовать шаги 2–6 «Порядка работы» из `removal-matrix.md`. Гейты: примеры «До/После» для clean (пара дельта = только маркеры, `check_examples.py`), витринные числа не трогаем (маркеров не добавляем), но если меняем число процедур — README RU/EN + CHANGELOG (инвариант 4). Демо и commands/ — не трогаем. Синхронизация обеих копий SKILL.md/references (инвариант 1).\n- **Предсказание.** На корпусе eval-markers (фикстуры с маркерами A/B + человеческие тексты): доля снятых маркеров класса A при полном нуле правок вне маркеров (проверяется повторным прогоном). Сигнал нужды: в слепых прогонах агент применяет полную перезапись там, где запрос был «только вычистить» (перекос в сторону humanize) — измерить долю таких расхождений между намерением и действием.\n- **Фальсификатор.** Прогон, где просьба «только убрать маркеры» завершилась стилистической правкой (нарушение режима), ИЛИ на тексте с единственным маркером A clean-режим изменил что-то кроме маркера. Не фальсификатор — «снял не все маркеры» (режим и так обязан повторить прогон).\n- **Цена ошибки.** Сделать без нужды: лишний текст в ядре (поверхность разночтений, расход контекста агента), обратимый мусор в справочниках — низкая. Не сделать при нужде: агент либо переписывает против воли (затирает живой голос — нарушение «не портить живое», худший случай), либо отказывает — цена выше, чем у лишней ветки.\n- **Обратимость.** Полная: откат текста SKILL.md + синхронизация копий + откат примера; версия-minor.\n- **Инварианты.** 1 (двойные копии), 4 (только если меняется счётчик), 6 (примеры До/После), 7 (clean не выдаёт вердиктов об авторстве — только «снято N»).\n\n## (б) Слэш-команда /clean рядом с /humanize и /audit\n\n- **Механика.** Новый `commands/clean.md` — стаб того же формата (description, argument-hint; тело — «удали из $ARGUMENTS только детерминированные артефакты класса A по SKILL.md, ничего не переписывай, выведи очищенный текст + список снятого»). README RU/EN: пункт команды + дерево каталогов + упоминание в списке архива (commands/ уже входит в release-бандл, allowlist не трогаем). Гейты: если заводим сверку «файлы commands/ == упомянутые команды» — новый валидатор с `--selftest` (инвариант 5). Ключевое: /clean без ветки (а) — пустой стаб, команда отсылает к правилам, которых нет; (б) = (а) + поверхность входа. commands/ **не** входит в инвариант зеркалирования (там только SKILL.md + references/*.md) — двойных копий не плодит.\n- **Предсказание.** Корпус запросов/issues: доля формулировок «хотел только убрать маркеры» + фиксация путаницы «/humanize переписал, а нужно было вычистить» (провал ожидания на существующих командах). Порог появления команды — заметная доля таких обращений без покрытия /audit (он не снимает) и /humanize (он переписывает).\n- **Фальсификатор.** Отсутствие в issues-истории обращений «только чистка» ИЛИ наличие устойчивого паттерна «/audit + ручная вычистка» без жалоб — команда не нужна; либо /clean сам становится источником запросов «а почему не переписал?» (размывание ожиданий).\n- **Цена ошибки.** Обе стороны дёшевы: не сделать = нет быстрого входа, но ветка (а) покрывает; сделать = лишний стаб в дистрибутиве и три команды вместо двух (микропутаница). Реальная цена — не (б), а (а) без (б) оставляет интенцию «только чистка» без явного входа; (б) без (а) — мёртвый код.\n- **Обратимость.** Полная и тривиальная: удалить `commands/clean.md` + строки README; версия-minor.\n- **Инварианты.** 4 (README RU/EN, если упоминаем), 5 (только при новом гейте команд). Формально 1 не затронут (commands/ не зеркалится).\n\n## (в) Указание в SKILL.md на filemarks для чистого текста\n\n- **Механика.** Сейчас `scripts/filemarks/` упомянут только для файлов с метаданными; чистый текст чистится агентом по справочнику (regEx/Grep). Расширение: в clean-ветке добавить «если вход — файл (.txt/.css/.js/.md…), детерминированный слой A может снять `scripts/filemarks/filemarks.py --clean`; повторный прогон для проверки; если скрипта в окружении нет — ручной режим по справочнику». Критичный нюанс: `filemarks.py` **не** зеркалируется в `src/humanizer_ru/` (check_pkg_sync покрывает только check_markers/scan_soft_signals) и, вероятно, отсутствует в dsh-копии `#path:/dsh` — указание обязано иметь фолбэк (а), иначе dsh-среда деградирует. Гейты: если всё же зеркалить filemarks в пакет — расширение check_pkg_sync и src/ (дорого: тянет container_meta/image_meta). Демо не трогаем (политика: переписывание/чистка — агент, не браузер; детерминированный слой в демо уже есть как инспекция).\n- **Предсказание.** Прямой замер на фикстурах с невидимыми символами (PUA U+E200–E204, нулевые пробелы, биди-контролы): дельта «ручной прогон по справочнику» vs «filemarks --clean» по доле снятых невидимых маркеров. Ожидание: агент физически не видит невидимки, Grep-подстроки ловят их частично → ручной режим <100% на классе «невидимые» — это и есть обоснование.\n- **Фальсификатор.** Фикстура с невидимыми маркерами, где ручной/Grep-режим по справочнику снял 100% без скрипта (тогда инструмент не нужен); ИЛИ фикстура, где `filemarks --clean` повредил текст (сработал guard не так, снял лишнее) и ручной режим оказался точнее — указание на скрипт вредно.\n- **Цена ошибки.** Не сделать: обещание «снимаем метки» для текста не выполняется детерминированно на невидимках — пропуски, которые пользователь не увидит; репутационный риск «не вычистил». Сделать: ядро обзаводится зависимостью от окружения (есть/нет scripts/) и размывается граница «текстовое ядро — агент; скрипты — CI» (AGENTS.md) — если систематически дёргать скрипты из SKILL.md, инвариант «пользователь не запускает scripts/» превращается в «агент запускает скрипты», что меняет модель доверия (выполнение кода в среде пользователя).\n- **Обратимость.** Средняя: строка в SKILL.md откатывается легко; зеркалирование filemarks в src/ + гейт — откат дороже и трогает пакет.\n- **Инварианты.** 1 (тексты обеих копий; при зеркалировании скриптов — расширение п.1), 5 (новый гейт), сам п.1 AGENTS.md (граница ядро/скрипты) — семантически под давлением.\n\n## (г) Отдельный режим = отдельный скилл\n\n- **Механика.** Новый бандл/репозиторий (например, `humanizer-ru-clean` или второй skill в dsh/): собственный SKILL.md, справочник маркеров (копия chatbot-artifacts или зависимость), своя витрина (числа=гейты, README RU/EN), свой CI (action + check_all + release-check), свое демо. Каждый новый маркер класса A → правка в двух репозиториях (или общий regex-пакет = новая зависимость, что снова связывает их). Конфликт с инвариантом 1: сейчас двойные копии, здесь — тройные/межреповые.\n- **Предсказание.** Замер на корпусе запросов: доля сценариев, где пользователь хочет ТОЛЬКО вычистку копипаста из чата и при этом не пересекается с аудиторией humanize (не нужен ни аудит, ни правка). Если доля высокая, а пересечение аудиторий низкое — разделение оправдано; синхронность обновлений словаря маркеров между двумя бандлами — контр-сигнал.\n- **Фальсификатор.** Высокое пересечение аудиторий: те же тексты проходят и чистку, и человезацию (тогда разделение = двойная работа); либо рассинхрон словаря: маркер добавлен в один скилл, а второй его не знает (детектор и чистильщик разошлись — ловится кросс-прогоном).\n- **Цена ошибки.** Самая высокая из четырёх. Сделать без нужды: дублирование словаря маркеров, два CI, два витринных набора, риск рассинхрона (лечится только общим пакетом regex, который снова привязывает бандлы друг к другу). Не сделать при нужде: узкая аудитория терпит лишние шаги, но покрывается (а)+/audit — цена умеренная. Асимметрия в пользу «не делать».\n- **Обратимость.** Низкая/средняя: создать просто, но если бандл обрастёт аудиторией — слияние обратно дорого (перенос справочников, история версий, витринные числа).\n- **Инварианты.** 1 (новые комплекты копий), 3 (конвейер маркера ×2 репо), 4 (витрина ×2), 5 (гейты ×2), плюс весь AGENTS.md в двух местах.\n\n---\n\n## Вывод по срезу\n\n- **Ядро — (а)**: обобщить существующую ветку «договор → только класс A» на явный запрос. Это минимальное изменение с полной обратимостью.\n- **(б) — только как обёртка над (а)**: /clean без ветки — пустой стаб; добавить одной строкой README + файл commands/ (не зеркалится — дёшево).\n- **(в) — опциональный ускоритель только для невидимых символов/файлов**, с обязательным фолбэком на ручной режим: пока не зеркалить filemarks в пакет, а зафиксировать в SKILL.md «если скрипт доступен — использовать, иначе по справочнику». Фальсифицируется замером «ручной режим снимает 100% невидимок».\n- **(г) — отклонять** до корпусного сигнала о самостоятельной аудитории: самая дорогая цена ошибки, тройное дублирование словаря, рассинхрон маркеров.\n\nИтоговая рекомендация: (а)+(б) одним паттерном, (в) — ограниченно, (г) — нет."
    },
    {
      "id": "hyp-adversarial",
      "model": "kimi/kimi-k3",
      "ok": true,
      "seconds": 201.9,
      "result": "# Adversarial-ревизия Q2 (humanizer-ru, S2)\n\nИсходная шкала: охват маркеров 3/24 = **12,5%**; лучший мягкий слой TP=7/24 = **29%** отзыва; FP гарда = 1 на пробном корпусе; B>D 14/14 — вердикт двух семейств LLM-судей на корпусе одной эпохи.\n\n## (а) Инверсии\n\n**А1. «Lint вреден не каннибализацией, а ложным обещанием»**\n- Механизм: lint продаёт «чисто от ИИ-следов», но снимает маркеры на 12,5% ИИ-файлов; мягкий слой (≤2 признаков/файл) он по определению не трогает. Продукт = «зелёная галка» на 87,5% немытого корпуса.\n- Предсказание: доля пользователей, считающих D-output «проверено, не ИИ», ≥ 50% при фактическом покрытии 12,5%.\n- Фальшификатор: прогнать 2–3 публичных детектора на D-output серии 1 → если вердикт «человек» для ≥ 20/24, обещание пустое.\n- Цена: выделить — репутационный удар при первом же разборе «ваш lint ничего не делает»; не выделить — потерян сегмент точечной чистки (реальный, но узкий).\n- Обратимость: низкая вверх (анлаунч продуктовой линии дороже отложенного лаунча).\n\n**А2. «14/14 — предпочтение судей, не пользователей»**\n- Механизм: LLM-судьи оценивают гладкость текста-на-тексте; человек оценивает сохранность своего голоса и нулевой риск фактов. Переписанный «голос» для автора своего текста — потеря, не выигрыш.\n- Предсказание: на слепой человеческой панели (n ≥ 30, свои тексты) выбор D вместо B ≥ 30–40%, против ~0% у LLM-панелей.\n- Фальшификатор: та же пара B/D, люди, свои тексты; если D < 15% — инверсия мертва.\n- Цена: поверить 14/14 — построить линию на прокси-метрике; отбросить 14/14 — проигнорировать единственный имеющийся сигнал качества.\n- Обратимость: высокая (панель — дешёвый эксперимент, решение откладываемо).\n\n**А3. «Идеальные гейты D — аргумент против B, не за выделение»**\n- Механизм: симметрия риска. D конструктивно не может исказить факты; B переписывает всё, включая человеческое, и каждая перезапись — ненулевой риск семантического дрейфа. Lint — единственный безопасный режим для смешанного/человеческого корпуса.\n- Предсказание: B на человеческих файлах даёт ≥ 1 семантическую правку на N файлов (N измерить); D даёт 0 по построению.\n- Фальшификатор: прогон B по человеческому корпусу с подсчётом дрейфа фактов.\n- Цена: без lint нет безопасного входа вообще; с lint — дробление поверхности поддержки.\n- Обратимость: высокая.\n\n**А4. «Дыра гарда U+2328 — не баг, а свойство класса»**\n- Механизм: обещание lint «человеческое не тронуто 100%» уже нарушено один раз на малом пробном корпусе; каждый новый regex-маркер несёт свой FP-rate. Гарды дрейфуют вместе с моделями.\n- Предсказание: FP-rate ≥ 1/100 файлов на расширенном человеческом корпусе (n ≥ 100).\n- Фальшификатор: расширить пробный корпус и посчитать FP; если 0/100+ — инверсия ослабевает.\n- Цена: FP в lint = порча чужого живого текста, в CI — блок чужого PR (хуже, чем пропуск: Главное правило скилла прямо предпочитает пропуск); ужесточение гардов = пропуск реальных маркеров.\n- Обратимость: средняя (гарды чинятся, доверие — нет).\n\n**А5. «Каннибализация наоборот: lint — воронка, а не конкурент»**\n- Механизм: пользователь пробует lint → видит остаточные проблемы (мягкий слой нетронут) → апгрейд до B. Наличие дешёвого входа увеличивает воронку B, а не съедает её.\n- Предсказание: конверсия lint→B > 0 в телеметрии; отказ от lint теряет входной сегмент целиком.\n- Фальшификатор: инструментировать путь использования; если перетока нет за когорту — инверсия мертва.\n- Цена в обе стороны зеркальна; обратимость высокая.\n\n**А6. «D>A 13/1/5 означает: lint — уже скрытый редактор»**\n- Механизм: если «только снятие артефактов» системно улучшает текст в глазах судей, то либо lint меняет больше, чем заявляет, либо судьи штрафуют A за маркеры. В обоих случаях рамка «lint = нейтральная чистка» ложна — это редакторский продукт, маскирующийся под гигиену.\n- Предсказание: дифф D vs A затрагивает > маркерных спанов (измерить долю изменённых токенов вне маркеров).\n- Фальшификатор: токен-дифф по серии 1.\n- Цена: построить позиционирование на ложной нейтральности; или избыточно усложнить описание.\n- Обратимость: высокая (документация).\n\n**А7. «Сегмент H0 ('только снять артефакты') — adversarial-отбор»**\n- Механизм: кому нужно снять именно метки поставщика, сохранив текст? В значительной доле — обходящим детекцию (академическая недобросовестность, ToS). Продукт для сегмента привлекает именно тех пользователей, чьи сценарии генерируют риск и чью проблему маркеры-то и решают на 12,5%.\n- Предсказание: концентрация abuse-сигналов/возвратов выше у lint-пользователей, чем у B.\n- Фальшификатор: опрос/телеметрия мотиваций.\n- Цена: выделить — стать «инструментом обхода»; не выделить — отказать и легитимным сценариям (очистка своего экспорта из чата).\n- Обратимость: низкая (ярлык липнет).\n\n## (б) Вне-рамочные\n\n**Б1. Линию выбирает канал дистрибуции, не репозиторий.** Скилл живёт в агентном контексте (натуральный акт — перезапись), action/ живёт в GitHub-маркете (натуральный акт — детекция/гейт). H1 и H2 не конкурируют за «одну линию» — это разные поверхности с разными JTBD; вопрос «выделять ли линию» предполагает единую продуктовую ось, которой нет. Предсказание: сплит использования по каналам ≈ 100/0. Фальшификатор: инструментация обоих входов. Цена: кросс-каннibalизация — артефакт рамки. Обратимость: полная.\n\n**Б2. Корпус структурно не может ответить на продуктовый вопрос.** 24 файла, 7 семей, одна эпоха: нет смешанного авторства, нет распределения реальных пользовательских текстов, нет временного дрейфа. G5 — это ответ на вопрос о способности (capability), а S2 — вопрос о спросе. Более того: max features=2 на ИИ-корпусе означает, что корпус *подозрительно чист* — либо тексты отредактированы, либо подсчёт консервативен; тогда и 12,5% охвата, и TP=7/24 — оценки на нерепрезентативной выборке. Предсказание: на полевых текстах обе метрики сместятся (направление неизвестно). Фальшификатор: принципиально не внутри корпуса — нужны полевые данные. Цена ошибки: принять capability-evidence за demand-evidence — главный провал всей постановки. Обратимость: н/п — вопрос снимается, не решается.\n\n**Б3. Вопрос преждевременен: evidence_pending — уже ответ.** S2 имеет статус, который и есть корректное решение: не коммитить продуктовую линию на незакрытых данных. Политика обновлений скилла сама разделяет устойчивое ядро и быстрый слой маркеров; продуктовая линия lint замораживает именно быстрый слой — то, что обязано дрейфовать. Предсказание: время жизни маркера ≈ одно поколение моделей (3–6 мес). Фальшификатор: трекинг hit-rate маркеров на новых релизах. Цена: преждевременный лаунч — необратимый naming/support; отсрочка — только стоимость ожидания. Обратимость асимметрична → отсрочка доминирует.\n\n**Б4. Рамка смешивает два разных продукта.** Если задача — обход детекторов: маркеры 3/24 + мягких ≤2 означают, что детекторы и так не ловят ≥ 87,5% корпуса; продукт решает проблему, которой измеримо нет. Если задача — качество текста: на неё отвечает B, и 14/14 релевантно. Это разные покупатели, этика и ответственность; S2 не называет, о каком из них вопрос. Фальшификатор: baseline-вердикты публичных детекторов на сыром A-корпусе. Цена: построить «обходной» продукт — репутационно токсично; построить «качественный» — тогда lint вообще не при чём. Обратимость: рамочная, полная.\n\n**Б5. Стоимость поддержки инвертирует интуицию.** Lint — это regex-реестр с реестром доказательств (3 образца на маркер, marker-sources.json), который протухает каждый релиз модели; B — это промпт-перезапись с нулевой поддержкой маркеров. «Дешёвый lint» — самая дорогая линия на единицу ценности. Предсказание: стоимость поддержки lint/пользователь растёт линейно с числом семейств моделей. Фальшификатор: учёт часов на маркер за релизный цикл. Обратимость: высокая.\n\n**Б6. «Линия» — импорт SaaS-логики в скилл-репозиторий.** В скилле нет упаковки и цены: «линия» = раздел README + точка входа. Реальное решение — топология документации, стоимость изменения ~0. Ставки рамки завышены; правильный ход — самый дешёвый обратимый (документировать D как режим, не как продукт). Цена ошибки в обе стороны мала → сам вопрос имеет низкий EV.\n\n## (в) Решения-ловушки\n\n1. **«Выделить lint, раз гейты идеальны»** — идеальность на механике ≠ ценность: продукт с охватом 12,5% решаемой проблемы.\n2. **«Не выделять, раз B>D 14/14»** — вердикт двух семейств LLM-судей (не независимых: обе семьи разделяют preference к гладкости; эффективный n < 2) на корпусе одной эпохи — не пользовательское предпочтение.\n3. **«CI-гейт как флаг ИИ/не-ИИ»** — прямо нарушает Главное правило скилла (вердикт по мягким признакам запрещён); гейт с TP=29% и уже одним FP (U+2328) продаст ложную уверенность и заблокирует чужой живой PR.\n4. **«Дожать T=2/K=1 и считать вопрос закрытым»** — TP=7/24=29% отзыва; «ждёт подписи» ≠ «станет лучше»; порог выбран под данных мало.\n5. **«Позиционировать lint как обход детекторов»** — репутационно/юридически токсично + гонка вооружений, где маркеры протухают каждое поколение моделей.\n6. **«Умный авто-режим»** — дерево уже мертво при T=3 (TP=0): расширение дерева без новых данных = накопление непроверенных веток.\n7. **«Решить вопрос ещё одной LLM-панелью»** — циркулярность: те же судьи, что дали 14/14; третья LLM-семья не добавляет независимости, добавляет видимость её.\n8. **«Оставить как есть навсегда»** — status quo как ловушка: накопленные факты (маркеры, корпус одной эпохи) имеют срок годности; evidence_pending без дедлайна тихо превращается в evidence_stale.\n\nРезюме ревизора: самая дешевая фальсифицируемая проверка — Б4/А1 (baseline детекторов + охват lint) и А2 (человеческая панель n≥30); обе дешевле, чем любое решение по S2, и обе способны снять вопрос вместо того, чтобы ответить на него."
    }
  ],
  "review": {
    "verdict": "25 уникальных гипотез после дедупликации и отбрасывания параметрических вариаций: null=3, mechanistic=12, inversion=6, out-of-frame=4.",
    "summary": "# Арбитраж Q2 (lint-only как продуктовая линия), фаза 1\n\n## Мастер-список (25 уникальных механизмов)\n\n**NULL (3)**\n- N1. lint = внутренний слой перезаписи/препроцессинг; ценность только как измеряемая дельта B′−B; фальсификатор — байт-идентичность B′≡B. [hyp-product в]\n- N2. Отделяемого спроса нет (пустой сегмент «не клиент»). Качественный null «lint бесполезен» УЖЕ фальсифицирован (D>A = 13/1/5). [hyp-segment кл.4]\n- N3. Вопрос преждевременен / низкий EV: evidence_pending и есть ответ, документировать D как режим, не продукт. [Б3+Б6]\n\n**MECHANISTIC (12)**\n- M1. lint-only «безопасная чистка» (prob-гарантия: доля предпочтений при запрете перезаписи). [product а]\n- M2. CI-автофикс «не только лови, но и чини» (точная байтовая гарантия). [product б]\n- M3. Позиционирование по носителю доказательства: инвариант (репо) vs человеческая панель (автор). [product г]\n- M4. Сегментация корпуса по осям маркеры(M)×мягкие(S) — 4 клетки, 1 свободный параметр. [hyp-segment]\n- M5. Каннибализация: порог удовлетворения + ценовой ореол замещают платную перезапись. [econ а]\n- M6. Воронка/индукция: дешёвый вход → конверсия в перезапись. [econ в ≡ A5]\n- M7. Гетерогенный JTBD: «пройти гейт» vs «улучшить текст» — разные сегменты спроса. [econ б]\n- M8. CI — другой рынок: покупатель = владелец пайплайна, zero cross-elasticity. [econ г]\n- M9. Базовая поверхность мутации шире контракта «0 на корпусе» (8+ классов живых текстов, guard-дыры, стрип метаданных). [safety а+б+в]\n- M10. CI-автолинт молчаливо опасен: cleaner шире детектора, нет backtick-guard, атомарная запись. [safety г]\n- M11. Выставить lint точкой входа в дереве (лестница: ветка→/clean→скрипт-hint = параметр). [integration а+б+в]\n- M12. Отдельный скилл/бандл: новый репо, тройной словарь, рассинхрон. [integration г]\n\n**INVERSION (6)**\n- I1. Ложное обещание: «зелёная галка» на 87,5% немытого (охват 3/24). [A1]\n- I2. 14/14 = предпочтение судей, не пользователей (для автора голос — потеря). [A2]\n- I3. Идеальные гейты D — аргумент ЗА lint, ПРОТИВ B (симметрия риска фактов). [A3]\n- I4. Дыра guard — свойство, не баг: FP-rate дрейфует с моделями. [A4]\n- I5. lint уже скрытый редактор (нейтральность ложна). [A6]\n- I6. Сегмент H0 = adversarial-отбор (обход детекции/ToS). [A7]\n\n**OUT-OF-FRAME (4)**\n- O1. capability ≠ demand: корпус меряет порядок качества, а не поведение выбора. [Б2 = econ-closing]\n- O2. Линию выбирает канал дистрибуции (скилл-агент vs action-маркет), единой оси нет. [Б1]\n- O3. Рамка смешивает два продукта (обход детекторов vs качество текста). [Б4]\n- O4. Стоимость поддержки инвертирует интуицию: regex-реестр протухает каждый релиз. [Б5]\n\n## Главные конкуренты гонки «выделять ли lint и как» (5)\n\n**C1 — null — «оставить внутренним слоем».** Механизм: ценность lint легитимна только как измеряемая дельта B′−B; отдельная линия добавляет поверхность без новой гарантии. Фальсификатор: B′≡B байт-в-байт на всех 14 и без снижения остаточных маркеров ⇒ слой чист.гигиена; ненулевая дельта/меньший разброс судей ⇒ слой окупается. ПРОВЕРЯЕМО на замороженном (прогон B vs B′ на 14, побайтовое сравнение + разброс судей).\n\n**C2 — mechanistic — «lint-only безопасный режим» (позитивная линия).** Механизм: продаёт НЕ-переписывание («убери мусор, стиль не тронь»); гарантия вероятностная = доля предпочтений под инструкцией «только удалить маркеры». Фальсификатор: ≤8/14 вин против исходника на артефактной подгруппе; meaning_loss>0; или B предпочтён даже при явном запрете перезаписи. ПРОВЕРЯЕМО на замороженном (D>A на 14 артефактных, 0/14 остаточных, human нетронут).\n\n**C3 — mechanistic — «CI-автофикс (байтовый инвариант)».** Механизм: ценность = три байтовых счётчика (0 остаточных / 0 изменённых чистых / 0 тронутых human), покупатель = владелец репо, «catch AND fix». Фальсификатор: любая единица инварианта (1 изменённый чистый или 1 тронутый human) убивает автофикс. ПРОВЕРЯЕМО: часть на замороженном (0/14, 0/5, 0/11 уже замерено; проба «cleaner шире детектора, нет backtick-guard» — замеряемо на скрипте); шаг action в чужом репо — ТРЕБУЕТ внешнего репо.\n\n**C4 — inversion — «lint уже скрытый редактор».** Механизм: D>A = 13/1/5 ⇒ либо lint меняет больше маркеров, либо судьи штрафуют маркеры ⇒ рамка «нейтральная чистка» ложна, это редактура под маской гигиены. Фальсификатор: token-diff D vs A — доля изменённых токенов вне маркерных спанов ≈0 (нейтральность держится) vs >0 (редактор). ПРОВЕРЯЕМО на замороженном (token-дифф — самый дешёвый решающий замер).\n\n**C5 — out-of-frame — «capability ≠ demand».** Механизм: корпус даёт порядок качества, а вопрос — о спросе/выборе; единой продуктовой оси между скилл-поверхностью (натуральный акт = перезапись) и action-поверхностью (натуральный акт = гейт) нет. Фальсификатор: в корпуса НЕ существует — вопрос снимается, не решается. ТРЕБУЕТ внешних данных (телеметрия сессий/конверсий/когорт, ценовой тест, инсталлы action).\n\nБлизкие конкуренты (не в топ-5): каннибализация vs воронка (M5/M6 — два знака одного ребра lint→перезапись, только внешняя телеметрия; знак решает, является ли C2 утечкой или верхом воронки); симметрия риска (I3 — прогнать B по 26 human, считать дрейф фактов, замороженный и дешёвый); abuse-отбор (I6 — внешний опрос, необратимый ярлык); канал решает линию (O2).",
    "risks": "# Пустые слоты и дефекты набора\n\n## Пустые/тонкие слоты\n- Качественный null «lint не имеет измеримого эффекта» — ПУСТ по существу: уже фальсифицирован (D>A = 13/1/5). В гонке остаётся только «lint = гигиена, не отделяемая линия» (N1-N3).\n- Гипотеза цены/WTP: упоминается ценовой тест (econ), но НЕТ ни одной гипотезы с ценовым механизмом/порогом.\n- Пороговая/объёмная гипотеза решения («отделять при N юзеров или X% сессий»): не выставлена нигде — у всех прогнозов нет числа-триггера.\n- Временной дрейф как измеряемый-в-репо счётчик: назван (Б3/Б5, shelf-life 3–6 мес), но нет в-репо фальсификатора — только трекинг hit-rate на будущих релизах (вне замороженного).\n- Юридический/комплаенс срез: зафлажен (safety-в: стрип EXIF/tEXt/лицензий «юридически значим»), но отдельной гипотезы нет — остаётся ремаркой внутри M9.\n\n## Дубли (объединены)\n- funnel: econ-в ≡ A5 (одинаковый механизм индукции) → M6.\n- «корпус не отвечает на спрос»: econ-closing ≡ Б2 → O1.\n- CI-рынок vs «канал решает линию»: econ-г ≈ Б1 — Б1 есть out-of-frame радикализация econ-г; оставлены обе (механистическая vs рамочная), связь о
… [truncated: 1873 more characters]