# Журнал прогонов Q2 (2026-08-28)

1. ~11:0x — фаза 1: генерация гипотез (6 исполнителей: 5 срезов
   deepseek-v4-flash-0731 + adversarial kimi/kimi-k3; арбитраж
   deepseek-v4-pro-0813 — 25 уникальных гипотез, конкуренты C1–C5).
   След — `evidence/phase1/hypotheses-fan.txt`.
2. ~11:1x — `registry.md` (H0, H-MODE, H-CI конкуренты; H-EDIT,
   H-DEMAND вне гонки) и `preregistration.md` (правило коллапса,
   прогнозы P1–P5, предусловие — фикс гарда до замера).
3. 11:22 — фикс гарда invisible_layout полным конвейером (ветка Q3,
   путь разблокировки 1): гард от всех эмодзи-баз + кейкап U+20E3;
   паритет md↔py; зеркало пакета; регенерация markers.v1.json и
   demo/markers.js. Предусловие предрегистрации исполнено.
4. 11:41 — заморозка: 82 файла (24 ИИ серии 1 + 9 пробы Q3 + 26 human
   + 12 adversarial + 2 boundary + манифесты + скрипты + реестр +
   предрегистрация + measure_q2.py v1; после импорт-фикса — v2/v3).
   Два исправления measure_q2.py ДО первых результатов (импорт
   text_layer; unpack lint_text) — прогонов с данными до v3 не было.
5. ~11:4x — **Прогон** measure_q2.py (канонический): P1 0/40, P2 0/9,
   P3 сегменты 3/0/0/30 (M+ = gemini/02, gemini/03, le-chat/01),
   P4 3/3 полнота снятия, P5 пустой S+. Все 5 прогнозов подтверждены.
6. ~12:0x — консилиум фазы 4 (qwen3.8-max + kimi/kimi-k3, арбитр
   deepseek-v4-pro-0813) → `consilium.json`: единогласен, H-CI.
   qwen независимо верифицировал заморозку (82/82) и хронологию.
7. ~12:1x — `decision.md`: коллапс в H-CI; внедрение fix-режима
   action/action.yml (опциональный вход `fix`, по умолчанию false;
   детекция класса A → чистка помеченных файлов → повторный скан).
8. ~12:3x — ревью фазы 5 (kimi/kimi-k3, полный раунд Q1+Q2+Q3):
   «условный reject» — блокер (живые ветки в разделе отколлапсированных
   — гейт падал) и CRLF в трёх файлах исправлены в ходе приёмки;
   major: M2 (форма исхода Q1 против буквы правила — переоформлено:
   вопрос переоткрыт, H0 остаётся), M3 (пост-якорь Q3 — пере-якорен),
   M4 (этот consilium.json + этот RUNLOG), M1 (коммит — финальный шаг).
   Smoke-прогон fix-логики action — перед коммитом.

Замечания ревизора, принятые как остаточные риски (в «Границы
внедрения» decision.md): байтовый no-op доказан в нормализованном
LF-пространстве (CRLF-файл чужого репо нормализуется при чистке);
паритет снятия на живом corpus — 2 формы маркеров; коллатеральные
правки вне спанов не измерялись (H-EDIT жив).

9. ~12:4x — smoke-прогон fix-логики action ПОЙМАЛ БАГ ВНЕДРЕНИЯ:
    CLI filemarks --clean маршрутизирует .md (дефолтный глоб action)
    в контейнерный путь (метаданные frontmatter), который НЕ снимает
    текстовые маркеры — gemini_span переживал чистку, повторный скан
    был красный. Исправлено до коммита: fix-цикл action вызывает
    текстовый путь напрямую (text_layer: clean_text_layer +
    clean_markup — путь, валидированный замерами Q2). Повторный
    smoke: gemini_span снят (markup=2), чистый файл не тронут,
    финальный скан exit 0. Правка отражена в decision.md (раздел
    «Внедрение»).
