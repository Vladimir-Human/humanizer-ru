# Q7: реестр гипотез — документ-уровневый слой

Дата: 2026-08-28. Фаза 1. Вопрос из внешнего ресёрча (спек ч. 6, Q7):
фразовая чистка не снимает документ-уровневые сигнатуры (структурная
burstiness, плотность тире, доля ИИ-цитат, параллельные секции); у
проекта документный уровень — только 4 оси ручного подсчёта в
`references/quantitative-heuristics.md`. Консолидирован капитаном из
предзаписанных веток спека; генерация не собиралась — вопрос
архитектурный, ветки предзаписаны.

## Конкуренты коллапса (участвуют в правиле)

```yaml
- id: H0
  slot: null
  statement: "Если ручного подсчёта четырёх осей достаточно (текущее
    состояние: quantitative-heuristics.md), документ-уровень остаётся
    справочником для агента, не автоматизируется"
  predictions: ["аудит quantitative-heuristics.md: 4 оси описаны с
    границами и без корпусной проверки (как заявлено)", "нет
    скриптового носителя документ-уровня в scripts/ (проверяемо)"]
  falsifiers: ["скриптовая ось документ-уровня уже существует — тогда
    H0 описывает не现状"]
  evidence_needed: ["аудит (этот цикл)"]
  cost_if_wrong: "документ-уровневые сигнатуры остаются невидимыми
    автоматике"
  cost_if_rejected_wrongly: "метрики повторят судьбу perplexity:
    корреляция без надёжности (аргумент Pangram) — строить нельзя"
  reversibility: "полная"
  gates_touched: []
  status: alive

- id: H-AUD
  slot: mechanistic
  statement: "Если добавить audit-режим с метриками документа (отчёт
    без правки — правка документ-уровня требует переписывания с
    пониманием контекста, детерминированно не снимается), агент
    получает наблюдаемые документ-уровневые сигналы; audit-режим уже
    существует как слэш-команда /audit"
  predictions: ["/audit уже заявлен в дистрибуции (CHANGELOG 3.15.0:
    слэш-команды /humanize и /audit)", "quantitative-heuristics.md —
    готовый источник осей для audit-режима"]
  falsifiers: ["/audit не существует", "оси без процедур подсчёта"]
  evidence_needed: ["аудит (этот цикл)"]
  cost_if_wrong: "audit-режим без валидированных метрик — числа ради чисел"
  cost_if_rejected_wrongly: "агент слеп к документ-структуре"
  reversibility: "высокая (текстовый слой + справочник)"
  gates_touched: ["check_bundle_sync"]
  status: alive

- id: H-REW
  slot: mechanistic
  statement: "Полноценный слой ПЕРЕЗАПИСИ структуры (документ-уровневая
    правка) — отдельная продуктовая линия"
  evidence_needed: ["validация документ-метрик на корпусе; дизайн
    правки структуры — вне этого цикла"]
  status: alive
  note: "в гонке не участвует — требует валидации H-AUD сначала"

- id: H-INV
  slot: inversion
  statement: "Документ-уровневые метрики повторят судьбу perplexity/
    burstiness — корреляция без надёжности (аргумент Pangram, 2025):
    не строить вовсе"
  evidence_needed: ["валидация метрик на замороженном корпусе: AUC
    документ-осей на разделении ИИ/человек"]
  status: alive
  note: "в гонке не участвует — фальсифицируем, но замер = отдельный
    цикл валидации; кадрируется как предусловие H-AUD"

- id: H-OOF
  slot: out-of-frame
  statement: "Вопрос не «считать ли метрики», а «что агент делает с
    результатом» — ценность в процедуре правки, не в числе"
  status: alive
  note: "рамка; в гонке не участвует"
```

## Слоты

null: H0. mechanistic: H-AUD, H-REW. inversion: H-INV.
out-of-frame: H-OOF. Все четыре слота заполнены.
