# Журнал прогонов Q7 (2026-08-28)

1. ~14:0x — `registry.md` + `preregistration.md` (правило, P1–P3);
   заморозка 54 файла.
2. ~14:1x — **Прогон 1** measure_q7.py: P1 частично (line-wrap баг
   строковой проверки: claim «не проходил корпусной проверки» есть,
   но переносится строкой — string-check дал False); P2 — МУСОР
   (баг: `for pat in ("**/*audit*")` без запятой итерирует СИМВОЛЫ
   строки — 125 мусорных путей с "..\..\.."); P3 корректен
   (детерминированная ветка).
3. ~14:2x — баги исправлены ДО признания результатов: (а) кортеж
   `("**/*audit*",)` с запятой; (б) нормализация переносов в P1-проверке.
   Скрипт ре-заморожен (v2); buggy-прогон сохранён как
   `stats-run1-p2-bug.json` (прозрачность, прецедент Q1).
4. **Прогон 2** (канонический): P1 ✓ (4 оси, ручные процедуры, claim
   найден), P2 ✓ (13 артефактов /audit, документ-уровень не
   автоматизирован: commands/audit.md — отчёт без правки), P3 ✓
   (rhythm_std=0.60, emdash_per_1k=0.58, list_frac=0.78 — три оси
   выше монеты; opener_top_frac=0.24 — анти-ориентация, см. decision).
   P1/P3 идентичны прогону 1 (не зависели от бага P2).
5. ~14:3x — консилиум (kimi/kimi-k3, арбитр deepseek-v4-pro-0813):
   H-AUD по букве правила; ревизор независимо воспроизвёл P3 (те же
   AUC), прочитал commands/audit.md напрямую; вскрыл баг P2 (125≠13,
   пути с "..") — исправлен ДО признания (п.3). Рекомендации ревизора
   исполнены: валидационные числа в справочник с оговорками, шапка
   справочника и SKILL.md обновлены, dsh-копии синхронны, ось 3 не
   инвертируется (оговорка), слой перезаписи не строится.
6. `decision.md`: коллапс в H-AUD.
7. Вечер: консолидированная фаза 5 для Q7/Q4/Q7 (зазор отдельного пост-коллапс ревью) — ACCEPT на всех уровнях, блокеров/мажоров нет; след — 2026-08-28-q4-judge-panel/evidence/phase5-closure-q457.md.  M3 (односемейный консилиум) принят с оговоркой в consilium.json.
