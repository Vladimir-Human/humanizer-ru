# Q5: реестр гипотез — водяной знак Claude

Дата: 2026-08-28. Фаза 1 протокола суперпозиции. Ветки предзаписаны
спеком (ч. 6, Q5) и BACKLOG; параллельная генерация гипотез не
собиралась — вопрос узкий, с заранее записанными исходами, поэтому
реестр консолидирован капитаном из предзаписанных веток + процедура
rewrite-guide.md.

Вопрос Q5: несёт ли генерируемый текст Claude (модели с 2026-08-02)
наблюдаемый водяной знак, и переживает ли он перезапись?

Приорное знание (опубликовано):
- llm-fingerprints.md:54 — заявление Anthropic (P-заявление): модели с
  2026-08-02 несут «встроенный невидимый водяной знак» (уровень модели,
  все площадки), переживает копирование, может сохраняться после правки;
  файлы получают C2PA. Образца знака нет; страница изменяемая.
- llm-fingerprints.md:58 — «отсутствие нулевых ширин по A.7 не
  доказывает отсутствие знака» (заявленный знак статистический,
  sampling-класс).
- rewrite-guide.md («Невидимая маркировка моделей», «Слой B») — процедура
  для текстов Claude: глубокая перезапись целиком моделью другого
  семейства; «знак снят» без верификатора не заявляется.
- Разблокировка: владелец предоставил канал добычи образца — провайдер
  AgentRouter (модель claude-opus-5, живой зонд 2026-08-28);
  BACKLOG-условие «как только будет дословный ответ Claude (модель от
  2026-08-02 и новее)» выполнимо.

## Конкуренты коллапса (участвуют в правиле)

```yaml
- id: H0
  slot: null
  statement: "Если кодпоинт-слой текста Claude чист (невидимых/служебных
    символов сверх фоновых нет), наблюдаемый знак в образцах отсутствует;
    заявленная статистическая метка остаётся за пределами текстового
    аудита (требует верификатора, которого нет) — процедура rewrite-guide
    без изменений, запись границы в реестр"
  predictions: ["побайтовый аудит 3 образцов Claude: 0 невидимых
    codepoints сверх фоновых (фон: 9 файлов пробы Q3 и 24 файла серии 1
    на тех же промптах)", "скан check_markers --scan: 0 маркеров"]
  falsifiers: ["систематический невидимый codepoint в образцах Claude,
    отсутствующий у других семейств на тех же промптах"]
  evidence_needed: ["живые образцы + побайтовый аудит (этот цикл)"]
  cost_if_wrong: "ложная запись 'знак не наблюдаем' при реально
    присутствующем codepoint-знаке"
  cost_if_rejected_wrongly: "ложная тревога: артефакт прокси принят
    за знак модели"
  reversibility: "полная (запись в реестр)"
  gates_touched: ["check_fixture_sources (строка реестра Claude)"]
  status: alive

- id: H-CODE
  slot: mechanistic
  statement: "Если кодпоинт-слой несёт след (систематический невидимый
    codepoint в образцах Claude, отсутствующий у других семейств на тех
    же промптах), знак наблюдаем механикой проекта: кандидат на
    регистрацию по полному конвейеру маркеров — с рамкой 'кандидат,
    не верифицированный знак поставщика'"
  predictions: ["в образцах Claude есть ≥1 невидимый codepoint с
    повторяемостью ≥2 из 3 образцов; в фоне (33 файла других семейств) — 0
    тех же codepoints"]
  falsifiers: ["codepoint встречается и в фоне (фоновой радиации
    достаточно — не маркер)", "единичное вхождение без повторяемости"]
  evidence_needed: ["живые образцы + аудит + фоновый контроль"]
  cost_if_wrong: "маркер-кандидат по артефакту прокси"
  cost_if_rejected_wrongly: "наблюдаемый знак не задокументирован"
  reversibility: "полная (конвейер маркеров)"
  gates_touched: ["полный конвейер AGENTS.md инвариант 3"]
  status: alive

- id: H-SURV
  slot: mechanistic
  statement: "Если кодпоинт-след (при наличии) переживёт детерминированный
    слой A, но снимется глубокой перезаписью моделью другого семейства —
    наблюдаемое поведение совпадает с документированной моделью слоёв
    (слой A детерминированный, слой B — перезапись)"
  predictions: ["слой A снимает codepoint-след (если он в LAYER_A/MARKUP
    наборах); перезапись не вносит новых невидимых"]
  falsifiers: ["след переживает перезапись (тогда рамка 'знак переживает
    правку' получает первое живое свидетельство)"]
  evidence_needed: ["исход H-CODE + перезапись + повторный аудит"]
  cost_if_wrong: "ожидаются по документации; отклонение информативнее"
  cost_if_rejected_wrongly: "—"
  reversibility: "полная"
  gates_touched: []
  status: alive
  note: "проверяется только при подтверждении H-CODE"
```

## Рамки (в гонке не участвуют)

```yaml
- id: H-INV
  slot: inversion
  statement: "Найденный codepoint-паттерн — артефакт прокси-канала
    (AgentRouter), а не знак модели: прокси может добавлять/терять байты;
    провенанс образца обязан это оговаривать"
  evidence_needed: ["прямой канал Anthropic для контраста (недоступен)"]
  status: alive
  note: "рамка-оговорка для любого исхода H-CODE"
- id: H-OOF
  slot: out-of-frame
  statement: "Без публичного верификатора вопрос 'переживает ли
    статистический знак перезапись' неразрешим в принципе: измерим только
    кодпоинт-слой и прокси-метрику перезаписи (delta), не сам знак;
    'знак снят' не заявляется — рамка rewrite-guide (best-effort)
    подтверждается либо пересматривается только в кодпоинт-части"
  evidence_needed: ["публичный верификатор (не существует)"]
  status: alive
  note: "рамка-граница для решения; смена рамки — отдельный вопрос"
```

## Слоты

null: H0. mechanistic: H-CODE, H-SURV. inversion: H-INV.
out-of-frame: H-OOF. Все четыре слота заполнены.

## Исход цикла (2026-08-28)

Коллапс состоялся (decision.md): **H0 — collapsed_winner** — кодпоинт-слой
чист на 3 образцах claude-opus-5 (0 невидимых codepoints сверх фоновых;
скан A.7 — 0 маркеров; слой A снял 0). H-CODE — dormant(убита замером:
повторяемость ≥2/3 недостижима при 0 невидимых; воскрешение: систематический
невидимый codepoint в образцах Claude, отсутствующий у других семейств).
H-SURV — dormant(вакатна по правилу — проверяется только при следе;
вспомогательное предсказание «перезапись не вносит новых невидимых»
подтверждено). H-INV и H-OOF — живы как рамки-ограничения записи
(провенанс прокси; статистический знак побайтово не наблюдаем).
Запись в реестр llm-fingerprints — уровень O; публичный вердикт
«знак снят/присутствует» не выносится.
