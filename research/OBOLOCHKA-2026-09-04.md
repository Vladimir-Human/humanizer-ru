# OBOLOCHKA-2026-09-04 — рабочий журнал потока «оболочка»

Приказ владельца 2026-09-04: довести оболочку до THE BEST, не тронув
честность ядра. Журнал ведётся по потокам W1-W10; факты — только с диска/API.

## Базовая линия (снята до правок, 2026-09-04)

- Опубликованная версия PyPI: 3.30.0 (PyPI JSON API).
- Тесты: локально `python -m unittest discover -s tests` — Ran 220 tests OK
  (skipped=1); в чистом venv sdist-test — 190 tests OK (skipped=2): часть
  тестов требует дерево репозитория и пропускается в пакете.
- Гейты: check_all quick 110/110 FAIL 0 на HEAD main после merge PR #76;
  полный check_all 121 гейт (110 в quick) — полное число снимается перед
  каждым релизом.
- Workflow main: последние прогоны зелёные, кроме закрытых fix(baseline)
  красного self-scan (чинится в PR #76 коммитом c9920a7).
- Открытые PR: #75 (dependabot: bump actions/deploy-pages 5.0.0 -> 5.0.1,
  файл .github/workflows/demo-pages.yml). Открытых issue: 0.
- GitHub About: «Нормализация и диагностика русского текста: артефакты
  копипасты, типографика без правки смысла, детектор связок со статусом
  домена. Числа — в реестре фактов с эрратой. Не для английского, кода,
  юрдокументов и художественной прозы»; Website: демо.
- PyPI summary: «CLI-сканеры следов машинной генерации русского текста
  (только stdlib)» — содержит запрещённые слова потока W1 (CLI, stdlib).
- Демо: title «humanizer-ru — статическое демо regex-слоя»; h1
  «humanizer-ru · regex-слой без сервера» — содержит «regex-слой» (W5).
- README первая строка: «# Humanizer-ru — нормализация и диагностика
  русского текста».
- Релизы: страница Releases вычищена аудитом 2026-09-04 до 8 содержательных
  (W-114); новый релиз по приказу оболочки — один после merge W1-W10.

## Поток W2 — открытый PR

- PR #75 (dependabot bump deploy-pages): отревьювлен, закрыт с комментарием
  5540854184: политика SHA-пинов (W6.4), эквивалент применён в PR #77.
- PR #77 (ci: deploy-pages v5.0.1 пином по SHA 368f825…): self-review
  5540853116, CI зелёный, слит (0a6bff4).
- Итог: открытых PR без ответа 0 (gh pr list --state open = 0).

## Поток W1 — единое позиционирование (закрыт)

- POSITIONING.md: короткая формула RU «Находит следы машинного текста в
  русском и объясняет их вам» (59 символов), длинная RU (154), зеркала EN;
  два режима «проверить» и «дочистить»; абзац «Почему так называется»;
  запрещённые слова формулы: regex, stdlib, CLI, слой, сканер.
- Применено дословно: pyproject description, README.md (вторая строка +
  абзац «Почему так называется», бюджеты 320 строк и own_style 79/80
  соблюдены), README.en.md, demo title/h1/meta/og/twitter, server.json
  description, CITATION.cff title/abstract, первая строка описания --help
  пяти CLI с argparse.
- Гейт scripts/check_positioning_sync.py в check_all (selftest с негативом);
  счётчики 123/112 во всех носителях.
- OWNER-TODO.md создан (7 пунктов с точными инструкциями), OWNER-ACTIONS.md
  помечен как история; POSITIONING.md и OWNER-TODO.md внесены в манифест
  check_docs (фикс после красного docs-джоба на PR, коммит на main).
- PR #78 слит (c50d4e4); self-review 5541020867.
- OWNER-TODO пункт 1 (About/Website) ждёт владельца: текст для вставки —
  короткая формула дословно.

## Поток W3 — визуал «за 3 секунды» (закрыт)

- assets/hero.svg (3053 байта, цикл 12 с): генерируется
  scripts/build_hero_svg.py из РЕАЛЬНОГО вывода humanizer-markers на
  образце; терминал с подсветкой находок и строкой «почему»; вставлен в
  README сразу под слоганом.
- Первый экран README по схеме: H1, слоган, hero, три бейджа (License,
  PyPI, CI main с ?branch=main); остальные бейджи в секции «Статус
  проекта» внизу; бюджет 320 строк сохранён.
- assets/social-preview.png 1280x640 (Pillow, детерминированно) +
  social-preview.svg; demo/og-image.png тем же дизайном; demo: link rel
  icon, og:image и twitter:image абсолютные на Pages-домен.
- assets/demo-screenshot.png: реальный скриншот демо headless-браузером
  (образец вставлен и проверен, 1280x800).
- PR #79 слит (f501f07); self-review 5541207822; красные self-scan на
  ветках починены fix-коммитами на main (литералы маркеров в build-скриптах
  экранированы, invalid escape в check_markers исправлен, зеркало синхронно).

## Поток W4 — первый экран «зачем» (закрыт)

- README.md и README.en.md переписаны по схеме: H1, слоган, hero, три
  бейджа, «Кому это нужно» (три роли с командой или ссылкой в один клик),
  «Попробовать за 30 секунд» (демо без установки, терминал с реальным
  выводом CLI, MCP одной конфигурацией), «Что это НЕ делает» (пункты
  дословно из THREAT-MODEL плюс честные границы polish и prohibited_uses),
  «Почему можно доверять» (методика/бенчмарк, threat-model, parity-гейт,
  self-audit), «Цифры проекта» со всеми витринными формулами, абзац
  «Почему так называется», оглавление.
- Длинные разделы и пара До/После уехали в docs/USAGE.md и
  docs/USAGE.en.md; README 96 строк вместо 320.
- Гейт scripts/check_readme_first_screen.py в CI: слоган, hero, не более
  трёх бейджей, слово «демо», pip install в первых 40 строках; секции
  первого экрана до «Цифр проекта»; selftest с негативом.
- Соседние гейты обновлены под новую структуру без ослабления: examples
  читает первую пару из USAGE, parity-списки и selftest-мутации по новой
  схеме, positioning и contract удовлетворены возвращёнными строками.
- PR #80 слит (df4bb3d); self-review 5541628432; CI зелёный включая self-scan.
- Счётчики 125/114 во всех носителях.

## Поток W5 — демо как продукт (закрыт)

- explain_ru у всех 40 маркеров: единственный источник
  scripts/marker_explain.py; export_markers пишет объект name/why/advice в
  markers.v1.json (опциональное поле, schema_version не поднят, обратная
  совместимость сохранена); generate_js_rules переносит в markers.js; демо
  показывает человекочитаемое имя, причину и совет в раскрывающемся блоке.
- Пустое состояние-приглашение и кнопка «Проверить свой»; итоговая строка
  «Найдено N следов в M предложениях. Это не приговор — смотрите
  объяснения.» без процентов вероятности; «Скопировать отчёт» (текст плюс
  JSON по контракту); «Поделиться» с hash не длиннее 8 КБ и предупреждением;
  Ctrl/Cmd+Enter; счётчик слов и символов; футер с версией правил, числом
  маркеров и датой сборки из мета-констант markers.js.
- demo/brand.css: токены бренда (два акцента, нейтральная палитра,
  системная шрифтовая пара), тёмная и светлая темы по prefers-color-scheme;
  manifest.json и service worker с кэшем, версионируемым хэшем правил,
  skipWaiting/claim для немедленного применения новых правил.
- Первый экран очищен от технических слов (regex, parity, имена
  генераторов) — всё в свёрнутом блоке «Для разработчиков»; гейт
  check_demo_a11y.py в CI (lang, aria-live, кнопки, placeholder, темы,
  manifest, SW, горячая клавиша, итоговая строка без процентов, чистота
  первого экрана; selftest с негативом).
- Проверено в headless-браузере: пустое состояние, объяснения по каждому
  маркеру, итоговая строка, ноль консольных ошибок (после сброса старого
  SW-кэша; причина ошибки — устаревший кэш markers.js, снято skipWaiting).
- CI-нюансы закрыты fix-коммитами на main: литерал `INSERT_SOURCE_URL` в
  объяснении разбит конкатенацией; scripts/marker_explain.py выведен из
  скоупа self-scan как реестр имён маркеров (данные, не продуктовый текст,
  по аналогии с исключёнными references/chatbot-artifacts*.md).
- PR #81 слит (398460f); self-review 5542079280; self-scan после фикса
  зелёный; счётчики 127/116 во всех носителях.

## Поток W6 — честный бейдж и честный CI (закрыт)

- workflow status.yml: docs/status.json и demo/status.json обновляются
  ТОЛЬКО после зелёных тестов и паритета main; упавший прогон файл не
  трогает, демо продолжает показывать дату последнего успеха; commit из CI
  с [skip ci] исключает рекурсию; permissions contents:write только у этого
  workflow; concurrency с cancel-in-progress.
- Демо показывает «проверено: дата, коммит» в футере (fetch status.json с
  обработкой отказа); README несёт ссылку на статус в секции доверия.
- Бейджи первого экрана: только License, PyPI и CI main с ?branch=main
  (сделано в W3); матричных и nightly-бейджей на первом экране нет.
- concurrency добавлен в action-smoke.yml; аудит: все workflow несут
  permissions и concurrency, все uses пинованы по SHA; невалидный пин
  setup-python убран из status.yml (раннер python3, как в остальных).
- CI-нюанс: коммит status.json из CI двигает main — учтён pull --rebase в
  локальном потоке; журнал и фиксы self-scan (бэктики вокруг литерала)
  закрыты fix-коммитами, все джобы main зелёные на d1bbc41.

## Поток W7 — PyPI (закрыт)

- pyproject: keywords человеческие (русский текст, машинный текст,
  редактура, проверка текста, ИИ-текст, артефакты копипасты, markdown);
  classifiers (Natural Language Russian, End Users/Desktop и Developers,
  Topic Text Processing Linguistic, Python 3.9-3.13); project.urls: Demo,
  Documentation, Changelog, Issues, Benchmark, Source.
- README.pypi: слоган второй строкой; команды дополнены humanizer-facts и
  humanizer-report; относительных изображений нет (hero не в PyPI-README,
  абсолютные ссылки не требуются).
- python -m build --sdist + twine check: PASSED локально.
- PR #82 слит (5b7c17c); self-review 5542232514.

## Поток W8 — от звезды к вкладу (закрыт)

- ISSUE_TEMPLATE: new-marker.yml (регулярка, три примера «за», три
  «против», источник с датой просмотра) и question.yml; config.yml с
  contact_links Discussions (включаются владельцем, OWNER-TODO пункт 2).
- CONTRIBUTING: раздел «Добавить маркер за 10 минут» (семь шагов конвейера:
  CASES/CLASS_OF, фикстуры и секция, реестр доказательств и
  REGISTERED_CASES, строка references, регенерация markers.v1.json и
  markers.js, гейты, PR); формат строки references уточнен примером после
  верификации.
- Верификация инструкции временным маркером test_temp_marker: дошла до
  парсера таблиц references (unpack 4 колонок), откат полный
  (git checkout по списку плюс удаление фикстур); сквозная верификация
  отложена до внешнего контрибьютора, заметка в BACKLOG.
- OWNER-TODO.md создан (About/Website, Discussions, pin, Glama, PyPI
  trusted publisher, MultiSocial, личная GPG-подпись акта) — единственный
  канал к владельцу.
- PULL_REQUEST_TEMPLATE: чек-лист unittest, parity, бюджеты обеих README
  поверх классов A/B/C и Главного правила.
- check_attribution: исключение CI-бота humanizer-ru-ci с selftest-кейсом
  (инфраструктурный коммитер status.yml; правило агентских пометок цело).
- good-first-issues #83-#88 созданы с меткой good first issue (отмена
  2026-09-03 снята приказом оболочки 2026-09-04, заметка в BACKLOG);
  черновики прошли check_outward 0 FAIL.
- PR #89 слит (eff4e28); self-review 5542888917; все checks зелёные.

## Поток W9 — публичный бенчмарк (закрыт)

- scripts/build_benchmark_page.py собирает таблицу из детерминированных
  снимков репозитория: fp-corpus (лёгкий и тяжёлый домены FP с Wilson CI),
  f8 (AUC мягких сигналов по поколениям), adversarial v2 (retention по
  операторам), marker-lr (доля маркеров без доказательной силы), реестр
  фактов (fact-loss); ни одного ручного числа.
- Артефакты: research/benchmark-2026-09-04/results.json, research/BENCHMARK.md,
  demo/benchmark/index.html (корень Pages, бренд-стили подключены).
- Колонка «где мы хуже» в каждой строке; абзац «Как обмануть humanizer-ru»
  со ссылкой на THREAT-MODEL (полная переписка снимает артефакты; стойкость к
  обходу не обещается и не измеряется, инвариант 2).
- Конкуренты: Imalwayshere/Open-Detector 238★ (BERT, английский академический
  текст — не воспроизводится на русском), talkstream/ru-text 223★ (набор
  правил без скоринг-процедуры — количественно не воспроизводится),
  smixs/humanizer-ru 148★ (форк самого проекта); статусы и методика указаны
  вместо воображаемого сравнения.
- Гейт scripts/check_benchmark.py: значения и интервалы страницы равны
  снимку, источники существуют, selftest с негативом подмены значения.
- Ссылки: README RU/EN (через demo/), футер демо; project.urls Benchmark в
  pyproject (W7) указывает на страницу.
- Счётчики 129/118; PR #90 слит (abd65aa); self-review перед merge; все
  джобы main зелёные после merge.

## Поток W10 — гигиена доверия (закрыт)

- SECURITY.md и SECURITY.en.md: секция «Сроки ответа» (подтверждение
  security-отчёта до 7 дней, статус каждые 7 дней до разрешения; обычные
  issue — best effort без отменённого ранее обещания 72 часа; приватные
  данные в открытых issue не публикуются).
- scripts/check_links.py (stdlib urllib, таймаут 10 с): проверка всех .md
  и docs; fenced-блоки и бэктики не считаются ссылками; локальные цели —
  только с путевым слэшем или известным расширением; сетевые ошибки —
  предупреждения, мёртвые статусы (404/410) — ошибка.
- Nightly-джоба md-links в link-check.yml (schedule и workflow_dispatch,
  permissions read): сетевые проверки не в PR-гейтах.
- Две реальные битые ссылки исправлены (SECURITY.md из docs/USAGE*.md);
  после правки check_links --offline даёт ноль мёртвых.
- CITATION.cff title уже несёт слоган (W1); CODE_OF_CONDUCT присутствует.
- PR #91 слит (2f769b7); self-review 5543292401; все джобы main зелёные.

## Фронтир-батч (закрыт) и релиз 3.31.0

- Фронтир-батч: F5a/F5b (сканер частей DOCX в filemarks: rels-targets с
  utm и песочницей, w:instrText, w:vanish, следы чат-интерфейсов в
  document/footnotes/comments/headers/footers/endnotes; 20 round-trip
  фикстур tests/test_docx_factloss.py с гейтом фактопотерь), F7'
  (tools/codepoint_probe.py: семь классов невидимых и служебных
  кодпоинтов, базовый замер research/CODEPOINT-PROBE-2026.md), F10/F15
  (секция SKILL.md «Профиль автора и уровни вмешательства»: уровни
  0/1/2, уровень 2 только по явной просьбе с отчётом фактопотерь; бюджет
  освобождён сжатием таблицы архитектуры, 15540 из 15750). PR #92 слит
  (e20d351), self-review 5543514698.
- Релиз 3.31.0 «оболочка: понятно с первого экрана»: полный check_all
  131/131 и unittest зелёные; версия во всех носителях включая скрытые
  манифесты плагинов (.claude-plugin, .codex-plugin, .cursor-plugin —
  скрытые каталоги не видны ripgrep по умолчанию, найдены полным
  обходом); self-audit регенерирован в релиз-коммите; подписанный тег
  v3.31.0 (c743272); детерминированный humanizer-ru.zip 562882 байт,
  sha256 8fa12fdb65562c1c9ab1ee4dc16d82598ee3ca50b0c3a24cbbd8856286b8a521
  совпал со сборкой CI; Release создан с заметками человеческого формата
  и hero.svg; «Проверка релиза» и «Публикация на PyPI» зелёные; PyPI
  3.31.0 подтверждён JSON API; Pages и /benchmark/ отдают 200.
- Нюансы зафиксированы в CHANGELOG Unreleased: pyproject keywords/urls
  обновлены на человеческие после публикации (поедут в следующем релизе,
  метаданные PyPI неизменяемы); тег указывает подписанный коммит до
  rebase поверх CI-коммита status.json, main несёт идентичный коммит
  0fd136d.

## Дальше по потокам

W1 позиционирование -> W3 визуал -> W4 README первый экран -> W5 демо ->
W6 честный CI -> W7 PyPI -> W8 вклад -> W9 бенчмарк -> W10 гигиена доверия
-> один релиз -> итоговый отчёт в этом журнале.
